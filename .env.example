# Local PostgreSQL, read by docker-compose.yml.
#
# Copy to .env and fill in. Values are never committed: this repository is public,
# and a committed secret is compromised even after it is deleted, because the
# commit remains reachable (CLAUDE.md, Secrets).

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=
