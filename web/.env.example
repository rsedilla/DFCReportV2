# Web client configuration.
#
# Copy to web/.env.local and fill in. Values are never committed (CLAUDE.md, Secrets).
# Everything the client needs is public by definition: it runs in a browser.

# Where the API is served, e.g. http://localhost:3001
NEXT_PUBLIC_API_URL=
