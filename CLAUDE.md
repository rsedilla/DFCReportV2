# G12 Church Management - Claude Project Instructions

## Source of Truth
Before planning or implementing any feature, read the repository `SKILL.md`.

`SKILL.md` is the authoritative source for G12 ministry rules, terminology, permissions, reporting definitions, attendance rules, and organizational behavior.

If an agent instruction conflicts with `SKILL.md`, `SKILL.md` wins.

## Architecture Principles
The thirteen non-negotiable principles are in `SKILL.md` §1. The platform architecture is in `SKILL.md` §2. Read them there.

They are deliberately not restated here. A second copy goes stale the moment `SKILL.md` changes, and a partial copy is worse than none, because it reads as if it were the whole list.

Rules that belong to this file rather than to `SKILL.md`:

- Do not duplicate domain rules into agents, prompts, or code comments. Reference `SKILL.md` by section.
- When any other instruction disagrees with `SKILL.md`, `SKILL.md` wins (Source of Truth, above).
- A decision that changes or settles a rule is recorded as a numbered ruling in `docs/decisions/`, indexed under Decisions below, **and** amended into `SKILL.md` in the same change. All three, or the work is unfinished. A decision that lives only in a chat session does not exist.

## Agent Coordination
Two agents are defined in `.claude/agents/`. Everything else is done inline in the main session or through built-in skills.

- **architecture-guardian** — read-only review of a change against `SKILL.md` domain invariants. Reports findings; does not edit.
- **qa-engineer** — writes and maintains tests, and owns the authorization test suite below.

Built-in skills cover the rest. Use `/security-review` for security review, `/code-review` for correctness and cleanup, and `/run` to launch the app. Do not create agents that duplicate them.

Do not spawn builder agents for UI, frontend, backend, data, or reporting work. A subagent starts cold and must re-read `SKILL.md` before it can apply a single domain rule, which costs more than doing the work sequentially in one session that already holds the context. Delegate only where a cold, independent read is the point — that is review, not construction.

Git and GitHub operations are performed inline. Revisit a dedicated integration agent once a repository exists.

## Mandatory Review
A change **must** receive `architecture-guardian` review before it is considered complete when it does any of the following:

- changes authorization, capabilities, or pastoral scope
- changes the pastoral hierarchy, or how a subtree is resolved
- changes person lifecycle, archival, or merge behaviour
- adds or changes a reporting metric, or how any total is counted
- changes database schema, constraints, or a migration
- changes attendance recording, correction, or the applicable-meeting calculation

Copy edits, styling, and changes confined to a single screen with no domain-rule impact do not require it.

## Definition of Done

A change is not complete until it is verified.

- Domain rules added or changed in `SKILL.md` have corresponding tests.
- Reporting changes include a reconciliation test asserting `SKILL.md` §20: classification buckets and monthly-attendance buckets must each sum to the same unique-people total. A reconciliation failure is a data-integrity defect, not a rounding issue.
- Authorization is tested at the API layer, not only the service layer, because the API is the sole authority for authorization (`SKILL.md` §7).
- Invariants that can be expressed as database constraints are verified to exist as database constraints, not only as application code.
- **A stage that ships an endpoint family no person can reach files a waiver for each such route in `web/screen-coverage.json`**, naming the stage that owes the screen. `web/scripts/check-screen-coverage.mjs` fails the build on a route the ledger does not mention, on a ledger entry no controller declares, on a waiver naming a stage the ledger does not declare, on a named screen that is not a file inside `web/`, **and on any controller or decorator it cannot parse** — a derivation that skipped what it could not read would claim a completeness a declared list never claimed. Shipping API-only is permitted and is often right — §2 fixes the API-first constraint — but it may not happen silently (ruling of 2026-09-06).
- **That stage also names the screens it owes, in prose, in `docs/ROADMAP.md`.** This half has nothing that can fail: the check reads the controllers and the ledger and never the roadmap, so a stage that files its waivers and writes nothing here is green. It is a separate bullet because the enforcement above does not reach it, and reading the two as one rule is what would let this half be assumed. A stage's bullet list is scope and its "Done when" is the exit criterion, and where the two diverge the bullets lapse with nothing recording it: Stage 3 delivered Cells and Stage 4 delivered attendance, and `web/app` reaches neither.

### Write endpoints

Four things a write endpoint owes, none of which anything detects. They are here
rather than only in `SKILL.md` §22 because a reviewer needs a list, and because
the three that concern the completion are invisible in a passing test suite.

- **It records its idempotency completion inside the transaction that performs
  the write** (`completeWithin`), never after it. Recording afterwards leaves a
  window where the write has committed and the claim has not been closed, and the
  claim lease then lets a retry perform the write again.
- **What it records is the response it returns.** A replay reproduces what was
  stored, not what was sent, so a divergence hands two identical requests two
  different answers. It follows that an endpoint must not commit its completion
  and then fail: the client would receive the failure while the store holds the
  success every retry replays.
- **The recording is the last statement in the transaction.** It takes the key's
  row lock, and a concurrent retry waits on that lock rather than being answered
  `REQUEST_IN_FLIGHT`.
- **It lets a lost claim abort the write.** `completeWithin` throws when it
  matches nothing; that exception is not caught and the transaction is rolled
  back. Swallowing it commits a write nothing recorded.

`api/test/api/idempotency.e2e.spec.ts` carries the exemplar to copy,
`records-its-own-completion`, and three probes that exist to break one rule each
so a test can catch it: `divergent-completion` records a body it does not return,
`rolls-back` fails after recording itself, and `slow-write` holds its transaction
open until its claim is taken. Each is labelled where it is written; none is the
shape to copy.

### Accessibility

`SKILL.md` §23 commits the web application to **WCAG 2.2 Level AA**. A conformance claim with nothing that can fail is a wish, so it is discharged in three parts.

- **The palette is checked on every build.** `web/scripts/check-contrast.mjs` computes 1.4.3 and 1.4.11 against the tokens in both themes and fails `npm run lint`. Contrast is decided by the palette, so a defect there is a defect on every screen at once, and no browser is needed to find it.
- **From the first real screen, axe-core runs in CI** over every route, and a violation fails the build. That arrives with Stage 2, because a browser harness for a placeholder page checks nothing. Automated rules catch only part of AA — treat a green axe run as the floor, not the ceiling.
- **A pull request that adds or changes a screen states how it meets the criteria automation cannot see**: keyboard operable end to end, with focus visible and the focused control never entirely obscured (2.4.7, 2.4.11); targets at least 24 by 24 CSS pixels (2.5.8); and, on the sign-in path, paste and password managers unobstructed (3.3.8). §23 names these alongside the two the contrast check covers.

Conformance concerns whether a person can perceive and operate the interface. It never licenses encoding meaning in colour: §13, §17 and §19 forbid encoding meeting status, coverage or a leader that way, whatever its contrast ratio.

### Migration policy

Migrations touch the history that `SKILL.md` guarantees is preserved. Treat every one as capable of destroying it.

- **Additive by default.** New columns are nullable or carry a default.
- **Never `DROP` a column or table holding historical relationship or attendance data.** Deprecate in place and stop writing to it. A column removed from the specification is not thereby removable from the database.
- **Reversible, or explicitly marked irreversible** and escalated as a Stop Condition before it runs.
- **A backfill that sets an effective date is backdating** (`SKILL.md` §5). It requires the same authorization, a reason, and an audit entry. Defaulting `started_at` on existing rows silently rewrites every historical report.
- **Validate constraints against existing data before enforcing them.** Adding the partial unique index to a table that already holds two active assignments for one person aborts mid-deploy. Find and fix the data first.
- **Snapshot before, reconcile after** for any migration touching `pastoral_assignments`, `cell_memberships`, `cell_leaderships`, network assignments, or attendance. Re-run the `SKILL.md` §20 reconciliation test on completion.
- **Constraint DDL is written by hand.** No ORM generates partial unique indexes, constraint triggers, or cycle-safe recursive queries; keep the SQL in the migration history rather than outside it.

### Authorization test suite

Pastoral assignment is the highest-risk authorization surface in the system (`SKILL.md` §5, Changing a person's pastoral leader). These cases must be pinned by tests and must stay green.

Using the example tree `Raymond -> Manuel -> Mark`:

1. Raymond cannot pull a person from a sibling branch into their own subtree.
2. Raymond cannot push a person out of their own subtree to a leader they do not oversee.
3. Raymond cannot change their own pastoral assignment.
4. Raymond cannot change the assignment of anyone upline of them.
5. Assigning a leader under one of their own descendants is rejected as a cycle.
6. An assignment whose leader and person belong to different Networks is rejected.
7. Two active assignments for one person are impossible, including under concurrent writes.
8. Bishop Oriel can reassign within the Women's Network, and Pastora Geraldine within the Men's.
9. Reassigning a leader moves their whole subtree with them, and no descendant assignment row is rewritten.
10. An archived Person cannot be reassigned.
11. A non-Admin cannot backdate an assignment's effective date.

Case 7 must be exercised concurrently, not only sequentially — a sequential test passes against application-layer checks alone and will not detect a missing database constraint.

## Working in this repository

### Branches and pull requests

`main` is protected. Work on a branch and open a pull request. Direct pushes are blocked for everyone but an administrator, and an administrator pushing directly to `main` is bypassing the review this file requires.

Name branches with a type prefix and a short description: `spec/cell-lifecycle`, `feat/attendance-api`, `fix/subtree-cycle`.

**Run `architecture-guardian` before requesting human review**, not after. If the change meets any Mandatory Review condition above, run it yourself and resolve what it reports. Arriving at review with its findings already addressed is the point of having it; asking a reviewer to discover them is not.

**Then run it again on what you changed in response.** A batch of fixes breaks something about as often as new writing does — measured on this repository, across four passes on two pull requests, every fix batch introduced defects of its own, including one that reinstated the exact failure the specification warns about. A review of the original that is not followed by a review of the fixes has checked the version nobody merged.

A pull request needs one approval. Changes to `SKILL.md`, `.claude/`, `.github/`, `docs/`, `api/migrations/`, and any `CLAUDE.md` or `AGENTS.md` **at any depth** additionally require a code owner (`.github/CODEOWNERS`). The last two are unanchored deliberately: an agent instruction file inside an app directory is read by every agent working there, and anchored patterns would have let one merge with neither owner seeing it.

Resolve every conversation before merging. Approvals are dismissed when new commits are pushed, so push fixes before asking for re-review.

Keep a pull request to one coherent change. A branch carrying six unrelated decisions is a branch nobody reads carefully.

### Commits

```text
<type>(<scope>): <summary, imperative, <= 72 characters>

<body: why, not what>
```

Types:

- `spec` — a change to `SKILL.md`, the domain source of truth
- `docs` — README and other non-normative documentation
- `feat`, `fix`, `refactor`, `test` — application code
- `chore` — tooling, configuration, CI

Scope is optional and names the area: `spec(cells)`, `feat(auth)`, `fix(reports)`.

**The body matters more than the subject.** A domain rule almost always has a reason that the diff does not show, and in six months the reason is the only part anyone needs. Write why the rule exists, and what was considered and rejected. A body that restates the diff is wasted.

Cite the specification section a commit implements or amends.

One commit per coherent change. Do not mix a rule change with unrelated tidying.

### Secrets

**This repository is public.** Nothing secret belongs in it, including in history. A committed secret is compromised even after it is deleted, because the commit remains reachable.

- No credentials, tokens, connection strings, or keys in any file, at any time.
- Configuration comes from the environment. Commit `.env.example` with variable names and no values. Never commit `.env`.
- Test fixtures use invented data. Never real member names, birthdays, or mobile numbers — the church holds records for minors, and a fixture is as public as the rest of the repository.
- If a secret is committed, **rotate it first and clean history second**. Rotation is the fix; removing the commit is tidying.

### Running the project

Two applications, each with its own dependencies: `api/` (NestJS) and `web/` (Next.js). Node 22 or later, and Docker for the local database.

```bash
cp .env.example .env                 # POSTGRES_USER, POSTGRES_PASSWORD, POSTGRES_DB
docker compose up -d                 # PostgreSQL 16, the minimum version (SKILL.md §5)

cd api && npm ci && cp .env.example .env
npm run migrate:up
npm run start:dev                    # http://localhost:3001/api/v1

# Two databases, not one. `npm test` truncates every table before every case, so
# the one it uses must never be the one holding data — the imported leadership
# tree, above all. Set TEST_DATABASE_URL to a scratch database and migrate it too:
#   DATABASE_URL=<the scratch url> npm run migrate:up
# Left empty it falls back to DATABASE_URL, which is right for CI and wrong here.

cd ../web && npm ci && cp .env.example .env.local
npm run dev                          # http://localhost:3000
```

`api/.env` and `web/.env.local` are not committed. `JWT_SECRET` must be at least 32 characters, and the application refuses to start without it rather than falling back to a default that would be wrong in production.

| Command | In | What it does |
| --- | --- | --- |
| `npm run lint` | `api`, `web` | ESLint. In `web` it also fails on an API route or a server action |
| `npm run typecheck` | `api`, `web` | `tsc --noEmit` |
| `npm run format:check` | `api` | Prettier |
| `npm test` | `api` | The suite that must stay green, the eleven authorization cases included. Needs a migrated database, and **truncates it before every case** — point `TEST_DATABASE_URL` at a scratch one |
| `npm run migrate:up` / `:down` / `:status` | `api` | Applies, reverts one, or lists migrations |
| `npm run validate:tree -- <file>` | `api` | Checks the leadership-tree CSV against everything decidable from the file alone. No database |
| `npm run import:tree -- --dry-run` / `--commit` | `api` | The two phases of the tree import (`SKILL.md` §2). `docs/TREE_CSV.md` carries the flags and what refuses a commit |
| `npm run bootstrap:admin` | `api` | Creates the first Admin account, once, and refuses while any account exists (§6) |
| `npm run build` | `api`, `web` | Production build |

There is no seed command. Section 2 of `SKILL.md` loads real data through the import flow rather than through fixtures, and test fixtures are built by the tests that need them.

Migrations are plain `.sql` files in `api/migrations/`, applied in filename order by `api/scripts/migrate.ts`. Each holds a `-- migrate:up` and a `-- migrate:down` section, and an applied migration is checksummed: editing one that has already run is refused, because the file and the database would otherwise disagree with nothing to say so. Write a new migration instead.

**One exception is in force, and it lapses per file.** A migration **not yet merged to `main`** may be corrected in place; one that has been merged is frozen and is corrected by writing a new migration. See the rulings of 2026-08-21 and 2026-09-01 below — the first granted this to `0001_foundations.sql` alone, and the second widened it to the fact the first was actually reasoning from. Rebuild your development database when the checksum refuses.

The line is merging rather than deployment because merging is the observable event: after it, the file is what every other branch builds from and what a reviewer has already read. Before it, a migration exists only on the branch writing it, and correcting the file there is cheaper and more honest than shipping a corrective migration for a defect found hours later in code nobody else has seen.

## Stop Conditions
Stop and request architectural clarification rather than inventing a rule when:
- `SKILL.md` does not define a required ministry rule.
- A requested change conflicts with an established rule.
- Authorization or pastoral scope is ambiguous.
- A reporting metric does not have an exact definition.
- A migration may destroy or rewrite historical data.

## Decisions

Rulings that shape the system and are not derivable from `SKILL.md` alone. Each entry records the decision, the date, and where it now lives in the specification.

This log is an index of rulings, never a substitute for `SKILL.md`. If a decision is here but not written into the specification, the work is unfinished.

The rulings are in `docs/decisions/`, one file each, numbered in the order they
were made. This index is what a reader scans; the file is where the reasoning is.
Adding a ruling means adding both, in the same change.

**They were written as one sequence and refer to each other positionally** —
"the ruling below", "the entry above", "superseded the same day". Nothing has been
rewritten to remove that language, because resolving each by inference is how a
false cross-reference gets introduced, and this log records enough of those
already. Each file carries previous/next links instead, so a positional reference
resolves exactly as well as it did in the single file and no better. *Recounted on
2026-09-10, the trigger being decision 0231: of 230 files, 229 carry `Previous:`, 181 carry
`Next:`, and 180 carry both on one line. 0213 through 0231 each moved the first two and
neither of the others, none of them carrying a `Next:`. The one file with no `Previous:` is
0001, which has nothing before it. The recount before this one was dated 2026-09-09 and
triggered by 0230, and was left stale by 0231 the next day — the interval is short because
the trigger is every added ruling, which is the point of naming the trigger rather than the
date. The one before that was dated the same day and triggered by 0229, and was left stale
by 0230 within hours. The recount before *that* was dated 2026-09-07 and was left stale by
0229 — by the commit whose stated purpose was correcting exactly this class. So the
commands are here rather than the instruction to recount being here alone, which is what the
Open section below already does with its own two counts:*

```bash
ls docs/decisions/*.md | wc -l                        # files
grep -l 'Previous:' docs/decisions/*.md | wc -l       # carry Previous:
grep -l 'Next:' docs/decisions/*.md | wc -l           # carry Next:
grep -lE 'Previous:.*Next:' docs/decisions/*.md | wc -l  # carry both on one line
```

*A claim that a command exists is worth exactly as much as the command, and a command is
worth what it actually prints. This paragraph asserted one for a commit before there was
one; the first four written to replace it anchored `Previous:` and `Next:` at the start of
a line and answered 1 where the prose says 181, because 180 of the files carry the pair on
one line. Both were caught by running them.*

*The stale figures said 214 and 213, and were left by the commits adding 0216 and 0217, and it was stale again by two when 0220 landed, 0218 and 0219 having each left it where it stood —
in the paragraph whose instruction is to recount rather than increment, which is why the
recount is dated and the trigger named rather than the numbers simply being replaced.* The forward half was
dropped after 0182 and 0173 never had one, so the chain is walkable backward
throughout and forward only as far as 0182 — worth repairing, not worth
restating the rule over. A batch on 2026-09-05 "corrected" this sentence to claim
backward links only, on an anchored `^Next:` grep that matched the single file
where the link sits on its own line; it replaced a true sentence with a false
one, in the paragraph whose subject is false cross-references, and
`architecture-guardian` caught it.*

- 2026-08-19 — [Who may reassign a person's pastoral leader](docs/decisions/0001-who-may-reassign-a-persons-pastoral-leader.md)
- 2026-08-19 — [Senior Pastors may reassign across both Networks](docs/decisions/0002-senior-pastors-may-reassign-across-both-networks.md)
- 2026-08-19 — [Network roots](docs/decisions/0003-network-roots.md)
- 2026-08-19 — [Agent roster reduced to two](docs/decisions/0004-agent-roster-reduced-to-two.md)
- 2026-08-19 — [Cell meeting status extended to three](docs/decisions/0005-cell-meeting-status-extended-to-three.md)
- 2026-08-19 — [Cell monthly denominator](docs/decisions/0006-cell-monthly-denominator.md)
- 2026-08-19 — [Attendance submission window](docs/decisions/0007-attendance-submission-window.md)
- 2026-08-19 — [Facilitation is not leadership](docs/decisions/0008-facilitation-is-not-leadership.md)
- 2026-08-19 — [Sorting permitted, ranking prohibited](docs/decisions/0009-sorting-permitted-ranking-prohibited.md)
- 2026-08-19 — [Reporting time zone is Asia/Manila](docs/decisions/0010-reporting-time-zone-is-asia-manila.md)
- 2026-08-19 — [Development reports DCC VIPs and Cell VIPs separately](docs/decisions/0011-development-reports-dcc-vips-and-cell-vips-separately.md)
- 2026-08-19 — [A merge lowers past-period totals](docs/decisions/0012-a-merge-lowers-past-period-totals.md)
- 2026-08-19 — [Role catalog](docs/decisions/0013-role-catalog.md)
- 2026-08-20 — [Stack pinned: NestJS, PostgreSQL, Next.js as a pure client](docs/decisions/0014-stack-pinned-nestjs-postgresql-next-js-as-a-pure-client.md)
- 2026-08-20 — [Three client surfaces used concurrently](docs/decisions/0015-three-client-surfaces-used-concurrently.md)
- 2026-08-20 — [DCC has no meeting status](docs/decisions/0016-dcc-has-no-meeting-status.md)
- 2026-08-20 — [Cell membership workflow](docs/decisions/0017-cell-membership-workflow.md)
- 2026-08-20 — [Duplicate matching rules](docs/decisions/0018-duplicate-matching-rules.md)
- 2026-08-20 — [Member ID generation](docs/decisions/0019-member-id-generation.md)
- 2026-08-20 — [No "on behalf" for pastoral assignment](docs/decisions/0020-no-on-behalf-for-pastoral-assignment.md)
- 2026-08-20 — [DCC submission window](docs/decisions/0021-dcc-submission-window.md)
- 2026-08-20 — [Archiving a Person who leads a Cell](docs/decisions/0022-archiving-a-person-who-leads-a-cell.md)
- 2026-08-20 — [Migration policy](docs/decisions/0023-migration-policy.md)
- 2026-08-20 — [Cell lifecycle, and closure is declared](docs/decisions/0024-cell-lifecycle-and-closure-is-declared.md)
- 2026-08-20 — ["Qualifies as a leader" means current Cell Leader](docs/decisions/0025-qualifies-as-a-leader-means-current-cell-leader.md)
- 2026-08-20 — [DCC monthly buckets derive from N](docs/decisions/0026-dcc-monthly-buckets-derive-from-n.md)
- 2026-08-20 — [Dashboard rules](docs/decisions/0027-dashboard-rules.md)
- 2026-08-20 — [Mobile number is the only contact detail; no email on a Person](docs/decisions/0028-mobile-number-is-the-only-contact-detail-no-email-on-a.md)
- 2026-08-20 — [Responsible leader for DCC attendance](docs/decisions/0029-responsible-leader-for-dcc-attendance.md)
- 2026-08-20 — [Cell ID generation](docs/decisions/0030-cell-id-generation.md)
- 2026-08-20 — [Cell attendance records members only](docs/decisions/0031-cell-attendance-records-members-only.md)
- 2026-08-20 — [API conventions](docs/decisions/0032-api-conventions.md)
- 2026-08-20 — [Cell schedule is effective-dated](docs/decisions/0033-cell-schedule-is-effective-dated.md)
- 2026-08-20 — [DCC events are generated ahead, not lazily](docs/decisions/0034-dcc-events-are-generated-ahead-not-lazily.md)
- 2026-08-20 — [Notifications go to the two Senior Pastors and their direct leaders only](docs/decisions/0035-notifications-go-to-the-two-senior-pastors-and-their-direct.md)
- 2026-08-20 — [Attention threshold is one church-wide Admin setting](docs/decisions/0036-attention-threshold-is-one-church-wide-admin-setting.md)
- 2026-08-20 — [Network is assigned from sex, not proposed](docs/decisions/0037-network-is-assigned-from-sex-not-proposed.md)
- 2026-08-20 — [Closed months may be materialized](docs/decisions/0038-closed-months-may-be-materialized.md)
- 2026-08-20 — [Backups are daily, not weekly](docs/decisions/0039-backups-are-daily-not-weekly.md)
- 2026-08-20 — [Two capabilities were referenced but never named](docs/decisions/0040-two-capabilities-were-referenced-but-never-named.md)
- 2026-08-20 — [DCC attendance is face to face only](docs/decisions/0041-dcc-attendance-is-face-to-face-only.md)
- 2026-08-20 — [Submission rolls up to the nearest upline with an account](docs/decisions/0042-submission-rolls-up-to-the-nearest-upline-with-an-account.md)
- 2026-08-20 — [Recorded scale and initial data load](docs/decisions/0043-recorded-scale-and-initial-data-load.md)
- 2026-08-20 — [Cell creation is request then approve](docs/decisions/0044-cell-creation-is-request-then-approve.md)
- 2026-08-20 — [Admin creates the initial Cells](docs/decisions/0045-admin-creates-the-initial-cells.md)
- 2026-08-20 — [Cell creation workflow, hardened](docs/decisions/0046-cell-creation-workflow-hardened.md)
- 2026-08-20 — [Initial encoding ends by an audited Admin action](docs/decisions/0047-initial-encoding-ends-by-an-audited-admin-action.md)
- 2026-08-20 — [A DCC attendance record requires a pastoral leader](docs/decisions/0048-a-dcc-attendance-record-requires-a-pastoral-leader.md)
- 2026-08-20 — [A person who changes Cell mid-month reports under the new Cell](docs/decisions/0049-a-person-who-changes-cell-mid-month-reports-under-the-new.md)
- 2026-08-20 — [A rescheduled meeting takes its roster from the actual date](docs/decisions/0050-a-rescheduled-meeting-takes-its-roster-from-the-actual-date.md)
- 2026-08-20 — [Notifications go to the direct leaders and Admin, not the Senior Pastors](docs/decisions/0051-notifications-go-to-the-direct-leaders-and-admin-not-the.md)
- 2026-08-20 — [`settings.manage` for church-wide operational settings](docs/decisions/0052-settings-manage-for-church-wide-operational-settings.md)
- 2026-08-20 — [A mid-month schedule change is resolved per week](docs/decisions/0053-a-mid-month-schedule-change-is-resolved-per-week.md)
- 2026-08-20 — [A calendar week begins on Monday](docs/decisions/0054-a-calendar-week-begins-on-monday.md)
- 2026-08-20 — [Monthly attendance is measured over the membership window](docs/decisions/0055-monthly-attendance-is-measured-over-the-membership-window.md)
- 2026-08-20 — [Cell monthly attendance reports on members](docs/decisions/0056-cell-monthly-attendance-reports-on-members.md)
- 2026-08-20 — [A schedule change takes effect the following month](docs/decisions/0057-a-schedule-change-takes-effect-the-following-month.md)
- 2026-08-20 — [Cell monthly attendance reverts to attendees, with a separate roster view](docs/decisions/0058-cell-monthly-attendance-reverts-to-attendees-with-a-separate.md)
- 2026-08-20 — [Three reporting questions are deferred to implementation](docs/decisions/0059-three-reporting-questions-are-deferred-to-implementation.md)
- 2026-08-20 — [Nine modules, each owning its tables](docs/decisions/0060-nine-modules-each-owning-its-tables.md)
- 2026-08-20 — [Every required structure is named and indexed](docs/decisions/0061-every-required-structure-is-named-and-indexed.md)
- 2026-08-20 — [The guard checks one target; the rest is domain layer](docs/decisions/0062-the-guard-checks-one-target-the-rest-is-domain-layer.md)
- 2026-08-20 — [`read_only` is valid only on a read capability](docs/decisions/0063-readonly-is-valid-only-on-a-read-capability.md)
- 2026-08-20 — [Migrations are hand-written SQL, and there is no ORM](docs/decisions/0064-migrations-are-hand-written-sql-and-there-is-no-orm.md)
- 2026-08-20 — [An endpoint that declares no capability is denied](docs/decisions/0065-an-endpoint-that-declares-no-capability-is-denied.md)
- 2026-08-20 — [Invariant 4 answers `SCOPE_DENIED`, not `INVARIANT_VIOLATION`](docs/decisions/0066-invariant-4-answers-scopedenied-not-invariantviolation.md)
- 2026-08-20 — [The eleven authorization cases ship failing, in their own CI job](docs/decisions/0067-the-eleven-authorization-cases-ship-failing-in-their-own-ci.md)
- 2026-08-20 — [A Network change validates forward from its effective date](docs/decisions/0068-a-network-change-validates-forward-from-its-effective-date.md)
- 2026-08-20 — [Three enforcement gaps closed at the schema, not in prose](docs/decisions/0069-three-enforcement-gaps-closed-at-the-schema-not-in-prose.md)
- 2026-08-20 — [The unauthenticated surface is a closed list, and `read_only` is not a role concept](docs/decisions/0070-the-unauthenticated-surface-is-a-closed-list-and-readonly-is.md)
- 2026-08-21 — [Tailwind CSS, chosen while there is one page to convert](docs/decisions/0071-tailwind-css-chosen-while-there-is-one-page-to-convert.md)
- 2026-08-21 — [UI direction: headless primitives the repository owns, and no design-system framework](docs/decisions/0072-ui-direction-headless-primitives-the-repository-owns-and-no.md)
- 2026-08-21 — [WCAG 2.2 Level AA, with something that can fail](docs/decisions/0073-wcag-2-2-level-aa-with-something-that-can-fail.md)
- 2026-08-21 — [Twelve findings from the Stage 1 verification, and why they existed](docs/decisions/0074-twelve-findings-from-the-stage-1-verification-and-why-they.md)
- 2026-08-21 — [Migration 0001 may be corrected in place until first deployment](docs/decisions/0075-migration-0001-may-be-corrected-in-place-until-first.md)
- 2026-08-21 — [Simultaneous presentation of a refresh token is not reuse](docs/decisions/0076-simultaneous-presentation-of-a-refresh-token-is-not-reuse.md)
- 2026-08-21 — [`account_roles` gains `senior_pastor_slot`](docs/decisions/0077-accountroles-gains-seniorpastorslot.md)
- 2026-08-21 — [A row of an effective-dated table is never deleted](docs/decisions/0078-a-row-of-an-effective-dated-table-is-never-deleted.md)
- 2026-08-22 — [A sign-in landing inside a revocation's transaction survives it](docs/decisions/0079-a-sign-in-landing-inside-a-revocations-transaction-survives.md)
- 2026-08-22 — [Seven Stage 2 rulings, settled before any Stage 2 code](docs/decisions/0080-seven-stage-2-rulings-settled-before-any-stage-2-code.md)
- 2026-08-22 — [Four enforcement gaps found reviewing the Stage 2 rulings](docs/decisions/0081-four-enforcement-gaps-found-reviewing-the-stage-2-rulings.md)
- 2026-08-22 — [A Network change is refused while the person leads anyone](docs/decisions/0082-a-network-change-is-refused-while-the-person-leads-anyone.md)
- 2026-08-22 — [`people.correct_sex`, the twenty-fifth capability, Admin-only](docs/decisions/0083-people-correctsex-the-twenty-fifth-capability-admin-only.md)
- 2026-08-22 — [Idempotency covers the authenticated write surface, and applies by default](docs/decisions/0084-idempotency-covers-the-authenticated-write-surface-and.md)
- 2026-08-22 — [A claim and a response are bounded separately](docs/decisions/0085-a-claim-and-a-response-are-bounded-separately.md)
- 2026-08-22 — [A write endpoint records its idempotency completion in its own transaction](docs/decisions/0086-a-write-endpoint-records-its-idempotency-completion-in-its.md)
- 2026-08-22 — [`people.create`, and how a Tier 1 duplicate is refused](docs/decisions/0087-people-create-and-how-a-tier-1-duplicate-is-refused.md)
- 2026-08-22 — [Three rulings the `people` module needed, all found by review](docs/decisions/0088-three-rulings-the-people-module-needed-all-found-by-review.md)
- 2026-08-22 — [A duplicate candidate outside the viewer's scope carries no tier](docs/decisions/0089-a-duplicate-candidate-outside-the-viewers-scope-carries-no.md)
- 2026-08-22 — [Membership of a candidate list is itself a disclosure](docs/decisions/0090-membership-of-a-candidate-list-is-itself-a-disclosure.md)
- 2026-08-23 — [Six rulings the sex-correction route needed, settled before the code](docs/decisions/0091-six-rulings-the-sex-correction-route-needed-settled-before.md)
- 2026-08-23 — [Reading the Network-change trigger fired twice, which is what the section 4 floor is about](docs/decisions/0092-reading-the-network-change-trigger-fired-twice-which-is-what.md)
- 2026-08-23 — [Three rulings the review of the sex correction forced, and one gap it found](docs/decisions/0093-three-rulings-the-review-of-the-sex-correction-forced-and.md)
- 2026-08-23 — [The root is a row, and a person lock serializes the same-Network rule](docs/decisions/0094-the-root-is-a-row-and-a-person-lock-serializes-the-same.md)
- 2026-08-23 — [`RESOURCE_BUSY`, and why its status carries the rule](docs/decisions/0095-resourcebusy-and-why-its-status-carries-the-rule.md)
- 2026-08-23 — [Three corrections to the lock, and two rules that were never written down](docs/decisions/0096-three-corrections-to-the-lock-and-two-rules-that-were-never.md)
- 2026-08-23 — [An identifier is compared canonically, and the class was wider than the instance](docs/decisions/0097-an-identifier-is-compared-canonically-and-the-class-was.md)
- 2026-08-23 — [A backdated reassignment is bounded by §4's floor and one rule of its own](docs/decisions/0098-a-backdated-reassignment-is-bounded-by-4s-floor-and-one-rule.md)
- 2026-08-23 — [The application runs at READ COMMITTED, and that is now load-bearing](docs/decisions/0099-the-application-runs-at-read-committed-and-that-is-now-load.md)
- 2026-08-23 — [Reusing a shape requires re-deriving why it has that shape](docs/decisions/0100-reusing-a-shape-requires-re-deriving-why-it-has-that-shape.md)
- 2026-08-23 — [Identifier normalization is global, and a pastoral leader has one field name](docs/decisions/0101-identifier-normalization-is-global-and-a-pastoral-leader-has.md)
- 2026-08-23 — [What an identifier's field name is, and the second walk over a body](docs/decisions/0102-what-an-identifiers-field-name-is-and-the-second-walk-over-a.md)
- 2026-08-24 — ["Never by layer" is about modules, not about files inside one](docs/decisions/0103-never-by-layer-is-about-modules-not-about-files-inside-one.md)
- 2026-08-24 — [Three rulings the accounts work needed, settled before the code](docs/decisions/0104-three-rulings-the-accounts-work-needed-settled-before-the.md)
- 2026-08-24 — [Four rulings the accounts review forced, and the escalation that prompted them](docs/decisions/0105-four-rulings-the-accounts-review-forced-and-the-escalation.md)
- 2026-08-24 — [The authorization seam is its own module, and a cycle was the reason a rule was being broken](docs/decisions/0106-the-authorization-seam-is-its-own-module-and-a-cycle-was-the.md)
- 2026-08-24 — [Who the two Senior Pastors are is read from configuration, and checked twice](docs/decisions/0107-who-the-two-senior-pastors-are-is-read-from-configuration.md)
- 2026-08-24 — [Naming a Senior Pastor takes effect on the next restart](docs/decisions/0108-naming-a-senior-pastor-takes-effect-on-the-next-restart.md)
- 2026-08-24 — [An account holds at most one of `ADMIN` and `SENIOR_PASTOR`](docs/decisions/0109-an-account-holds-at-most-one-of-admin-and-seniorpastor.md)
- 2026-08-24 — [The grant-making pair is never held by a Senior Pastor](docs/decisions/0110-the-grant-making-pair-is-never-held-by-a-senior-pastor.md)
- 2026-08-24 — [How the leadership tree import runs](docs/decisions/0111-how-the-leadership-tree-import-runs.md)
- 2026-08-24 — [Birthday is optional on a Person](docs/decisions/0112-birthday-is-optional-on-a-person.md)
- 2026-08-25 — [The decisions file is a CSV, and the fingerprint is over trimmed fields in order](docs/decisions/0113-the-decisions-file-is-a-csv-and-the-fingerprint-is-over.md)
- 2026-08-25 — [A root has a seat, and a nullable leader could not say what it meant](docs/decisions/0114-a-root-has-a-seat-and-a-nullable-leader-could-not-say-what.md)
- 2026-08-25 — [The tree is known centrally only to its first level, and no birthday is required](docs/decisions/0115-the-tree-is-known-centrally-only-to-its-first-level-and-no.md)
- 2026-08-25 — [A generational suffix lives in `last_name`, and a title lives nowhere](docs/decisions/0116-a-generational-suffix-lives-in-lastname-and-a-title-lives.md)
- 2026-08-25 — [The first Admin account is a one-time command, and an administrator need not be in the tree](docs/decisions/0117-the-first-admin-account-is-a-one-time-command-and-an.md)
- 2026-08-26 — [A module's tables are never written by another, and read by one only where the query is rooted elsewhere](docs/decisions/0118-a-modules-tables-are-never-written-by-another-and-read-by.md)
- 2026-08-26 — [The bootstrap's two service methods guard themselves, and `ts-node` ships](docs/decisions/0119-the-bootstraps-two-service-methods-guard-themselves-and-ts.md)
- 2026-08-26 — [The tree import, and the one thing the fingerprint cannot bind](docs/decisions/0120-the-tree-import-and-the-one-thing-the-fingerprint-cannot.md)
- 2026-08-26 — [The import's actor must hold ADMIN, and four other findings from the review](docs/decisions/0121-the-imports-actor-must-hold-admin-and-four-other-findings.md)
- 2026-08-26 — [A check that reads what its caller handed it is not a check](docs/decisions/0122-a-check-that-reads-what-its-caller-handed-it-is-not-a-check.md)
- 2026-08-26 — [Advice printed at the moment of a decision, and a fix claimed but not made](docs/decisions/0123-advice-printed-at-the-moment-of-a-decision-and-a-fix-claimed.md)
- 2026-08-27 — [What the web client does with a refresh token, pending three rulings](docs/decisions/0124-what-the-web-client-does-with-a-refresh-token-pending-three.md)
- 2026-08-27 — [Every tab of one browser profile is one session](docs/decisions/0125-every-tab-of-one-browser-profile-is-one-session.md)
- 2026-08-27 — [`field-invalid` follows the field, not the error code](docs/decisions/0126-field-invalid-follows-the-field-not-the-error-code.md)
- 2026-08-27 — [An idempotency key belongs to a body, not to an attempt](docs/decisions/0127-an-idempotency-key-belongs-to-a-body-not-to-an-attempt.md)
- 2026-08-27 — [A re-presentation whose replacement was never used is a retry](docs/decisions/0128-a-re-presentation-whose-replacement-was-never-used-is-a.md)
- 2026-08-28 — [Membership and order disclose as loudly as fields did](docs/decisions/0129-membership-and-order-disclose-as-loudly-as-fields-did.md)
- 2026-08-28 — [Two engines, and the width argument gets something that can fail](docs/decisions/0130-two-engines-and-the-width-argument-gets-something-that-can.md)
- 2026-08-28 — [A pastoral path says which end is a root](docs/decisions/0131-a-pastoral-path-says-which-end-is-a-root.md)
- 2026-08-28 — [Three rulings before Stage 3, and a fourth withdrawn](docs/decisions/0132-three-rulings-before-stage-3-and-a-fourth-withdrawn.md)
- 2026-08-28 — [A Cell changes hands by request and approval, and a closure is never reversed](docs/decisions/0133-a-cell-changes-hands-by-request-and-approval-and-a-closure.md)
- 2026-08-28 — [The Cell schema, and a test that agreed with itself on one machine only](docs/decisions/0134-the-cell-schema-and-a-test-that-agreed-with-itself-on-one.md)
- 2026-08-29 — [Direct creation, and a subtree check where Section 2 asks for Whole Church](docs/decisions/0135-direct-creation-and-a-subtree-check-where-section-2-asks-for.md)
- 2026-08-29 — [A Cell is placed in the tree by its leader, and a move is two changes](docs/decisions/0136-a-cell-is-placed-in-the-tree-by-its-leader-and-a-move-is-two.md)
- 2026-08-29 — [Six rulings the closure endpoint needed, and two the review raised](docs/decisions/0137-six-rulings-the-closure-endpoint-needed-and-two-the-review.md)
- 2026-08-29 — [A second schedule change corrects the pending one](docs/decisions/0138-a-second-schedule-change-corrects-the-pending-one.md)
- 2026-08-29 — [The closure ordering and the closure floor, settled by running the database](docs/decisions/0139-the-closure-ordering-and-the-closure-floor-settled-by.md)
- 2026-08-29 — [Twelve findings on the closure, and the three the review escalated](docs/decisions/0140-twelve-findings-on-the-closure-and-the-three-the-review.md)
- 2026-08-29 — [Ten more on the fixes, and the one the fixes introduced](docs/decisions/0141-ten-more-on-the-fixes-and-the-one-the-fixes-introduced.md)
- 2026-08-29 — [Ten on the second fix batch, and the rule written with nothing that could fail on it](docs/decisions/0142-ten-on-the-second-fix-batch-and-the-rule-written-with.md)
- 2026-08-29 — [Seven on the third fix batch, and a 500 on a documented parameter](docs/decisions/0143-seven-on-the-third-fix-batch-and-a-500-on-a-documented.md)
- 2026-08-29 — [Five on the fourth fix batch, and a bound that moved underneath its payload](docs/decisions/0144-five-on-the-fourth-fix-batch-and-a-bound-that-moved.md)
- 2026-08-29 — [Four on the fifth fix batch, and a disjunction pinned with a member missing](docs/decisions/0145-four-on-the-fifth-fix-batch-and-a-disjunction-pinned-with-a.md)
- 2026-08-29 — [Five on the sixth fix batch, all of them what the batch said about itself](docs/decisions/0146-five-on-the-sixth-fix-batch-all-of-them-what-the-batch-said.md)
- 2026-08-30 — [A requester may decline their own request, and a decision is final](docs/decisions/0147-a-requester-may-decline-their-own-request-and-a-decision-is.md)
- 2026-08-30 — [Three small settlements from building step one of the request workflow](docs/decisions/0148-three-small-settlements-from-building-step-one-of-the.md)
- 2026-08-30 — [Section 10's "at any scope" was resting on a scope value](docs/decisions/0149-section-10s-at-any-scope-was-resting-on-a-scope-value.md)
- 2026-08-30 — [Ten on the request fix batch, and a fix claimed in the past tense that was never made](docs/decisions/0150-ten-on-the-request-fix-batch-and-a-fix-claimed-in-the-past.md)
- 2026-08-30 — [Four on the second fix batch, and a pin that pinned nothing](docs/decisions/0151-four-on-the-second-fix-batch-and-a-pin-that-pinned-nothing.md)
- 2026-08-30 — [Approval records the leadership and leaves the account pending](docs/decisions/0152-approval-records-the-leadership-and-leaves-the-account.md)
- 2026-08-30 — [Three rulings the approval endpoint needed, and a condition nothing could evaluate](docs/decisions/0153-three-rulings-the-approval-endpoint-needed-and-a-condition.md)
- 2026-08-30 — [A Network change is refused while the person leads a Cell](docs/decisions/0154-a-network-change-is-refused-while-the-person-leads-a-cell.md)
- 2026-08-30 — [The backdate floor gains two Cell terms, one per mechanism](docs/decisions/0155-the-backdate-floor-gains-two-cell-terms-one-per-mechanism.md)
- 2026-08-30 — [`DateStyle` is pinned by the connection, not inherited](docs/decisions/0156-datestyle-is-pinned-by-the-connection-not-inherited.md)
- 2026-08-31 — [A Cell leadership audit entry names the Cell](docs/decisions/0157-a-cell-leadership-audit-entry-names-the-cell.md)
- 2026-08-31 — [A stale premise under a cleanly taken lock is transient](docs/decisions/0158-a-stale-premise-under-a-cleanly-taken-lock-is-transient.md)
- 2026-08-31 — [A cursor that cannot be resolved is refused](docs/decisions/0159-a-cursor-that-cannot-be-resolved-is-refused.md)
- 2026-08-31 — [One API instance, and the skew bound waits for the second](docs/decisions/0160-one-api-instance-and-the-skew-bound-waits-for-the-second.md)
- 2026-08-31 — [The DCC calendar is advanced by a scheduled command](docs/decisions/0161-the-dcc-calendar-is-advanced-by-a-scheduled-command.md)
- 2026-08-31 — [A Cell meeting has no row until it is reported](docs/decisions/0162-a-cell-meeting-has-no-row-until-it-is-reported.md)
- 2026-08-31 — [A Cell meeting's responsible leader is frozen as of the meeting](docs/decisions/0163-a-cell-meetings-responsible-leader-is-frozen-as-of-the-meeting.md)
- 2026-08-31 — [A Cell submission versions the meeting; a DCC submission versions the person](docs/decisions/0164-a-cell-submission-versions-the-meeting-a-dcc-submission-versions-the-person.md)
- 2026-08-31 — [Four Stop Conditions the Stage 4 rulings raised](docs/decisions/0165-four-stop-conditions-the-stage-four-rulings-raised.md)
- 2026-08-31 — [Three more the second review of the Stage 4 rulings raised](docs/decisions/0166-three-more-the-second-review-of-the-stage-four-rulings-raised.md)
- 2026-08-31 — [Four more from the third review, including a key that was wrong](docs/decisions/0167-four-more-from-the-third-review-including-a-key-that-was-wrong.md)
- 2026-08-31 — [The closed-month back-fill is withdrawn](docs/decisions/0168-the-closed-month-back-fill-is-withdrawn.md)
- 2026-08-31 — [Four that the closed-Cell path produced](docs/decisions/0169-four-that-the-closed-cell-path-produced.md)
- 2026-08-31 — [The submission window runs through the whole of the 7th](docs/decisions/0170-the-submission-window-runs-through-the-whole-of-the-7th.md)
- 2026-08-31 — [Four rulings the DCC recording path needed, settled before the code](docs/decisions/0171-four-rulings-the-dcc-recording-path-needed.md)
- 2026-08-31 — [Who submits a person's DCC attendance, and where a root's is recorded](docs/decisions/0172-who-submits-a-persons-dcc-attendance.md)
- 2026-08-31 — [Seven settlements from building DCC recording](docs/decisions/0173-seven-settlements-from-building-dcc-recording.md)
- 2026-08-31 — [The fourth cursor, and the two that share a key](docs/decisions/0174-the-fourth-cursor-and-the-two-that-share-a-key.md)
- 2026-08-31 — [Eleven on the DCC recording review, and the one that was a disclosure](docs/decisions/0175-eleven-on-the-dcc-recording-review-and-the-one-that-was-a-disclosure.md)
- 2026-08-31 — [Eight on the fix batch, and the outcome that was a 500](docs/decisions/0176-eight-on-the-fix-batch-and-the-outcome-that-was-a-500.md)
- 2026-08-31 — [Six on the third pass, and a fix that was never applied](docs/decisions/0177-six-on-the-third-pass-and-a-fix-that-was-never-applied.md)
- 2026-08-31 — [The fourth pass found nothing behavioural, and one thing with teeth](docs/decisions/0178-the-fourth-pass-found-nothing-behavioural-and-one-thing-with-teeth.md)
- 2026-08-31 — [A symmetry that was not there, and the index a claim needed](docs/decisions/0179-a-symmetry-that-was-not-there-and-the-index-a-claim-needed.md)
- 2026-09-01 — [A migration is frozen by merging, not by its number, and two on the seventh pass](docs/decisions/0180-a-migration-is-frozen-by-merging-not-by-its-number.md)
- 2026-09-01 — [A port is optional and refuses, and the graph test asserts it is bound](docs/decisions/0181-a-port-is-optional-and-refuses-and-the-graph-test-asserts-it.md)
- 2026-09-01 — [A closed month is amended on the routes that record it, and §20 already said who invalidates](docs/decisions/0182-a-closed-month-is-amended-on-the-routes-that-record-it.md)
- 2026-09-01 — [A record closed with nothing replacing it names itself](docs/decisions/0183-a-record-closed-with-nothing-replacing-it-names-itself.md)
- 2026-09-01 — [A Cell meets on the day it was created, and the bound is a date at both ends](docs/decisions/0184-a-cell-meets-on-the-day-it-was-created.md)
- 2026-09-02 — [A date-only field that is not a day is refused at the edge, by one predicate](docs/decisions/0185-a-date-only-field-that-is-not-a-day-is-refused-at-the-edge.md)
- 2026-09-02 — [The capability decides a meeting's scope resolution, not the HTTP method](docs/decisions/0186-the-capability-decides-a-meetings-scope-not-the-http-method.md)
- 2026-09-02 — [A handover on a meeting's own day leaves the meeting with the outgoing leader](docs/decisions/0187-a-handover-on-a-meetings-own-day-leaves-the-meeting-with-the-outgoing-leader.md)
- 2026-09-02 — [A meeting's scope resolves through its frozen responsible leader](docs/decisions/0188-a-meetings-scope-resolves-through-its-frozen-responsible-leader.md)
- 2026-09-03 — [A Cell attendance audit entry targets the Cell, a DCC one the Person](docs/decisions/0189-a-cell-attendance-audit-entry-targets-the-cell.md)
- 2026-09-03 — [A Cell meeting has one write operation, and `cell_attendance.version` is not compared](docs/decisions/0190-a-cell-meeting-has-one-write-operation.md)
- 2026-09-03 — [A write that writes nothing owes no *amendment* capability, and its confirmation is a disclosure](docs/decisions/0191-a-write-that-writes-nothing-owes-no-further-capability.md)
- 2026-09-03 — [`cell.submit_on_behalf` is required, and measured against the leader the meeting resolves through](docs/decisions/0192-cell-submit-on-behalf-is-measured-against-the-leader-the-meeting-resolves-through.md)
- 2026-09-03 — [Section 7's contents-ordering rule binds DCC, and the correction-reason refusal moves behind it](docs/decisions/0193-section-7s-contents-ordering-rule-binds-dcc.md)
- 2026-09-03 — [The accepted disclosure is a Cell argument, and the DCC roster publishes per-person figures by design](docs/decisions/0194-the-accepted-disclosure-is-a-cell-argument.md)
- 2026-09-04 — [A reschedule carries the roster of the day it moved to, in one operation](docs/decisions/0195-a-reschedule-carries-the-roster-of-the-day-it-moved-to.md)
- 2026-09-04 — [Two the ninth pass found, and a Section 13 clause the schema could not hold](docs/decisions/0196-two-the-ninth-pass-found-and-a-clause-the-schema-could-not-hold.md)
- 2026-09-04 — [The fix batch for 0196 broke two of the rules 0196 wrote](docs/decisions/0197-the-fix-batch-for-0196-broke-two-of-the-rules-0196-wrote.md)
- 2026-09-04 — [A field that takes text refuses what cannot be stored, everywhere, and a check derives where](docs/decisions/0198-a-field-that-takes-text-refuses-what-cannot-be-stored.md)
- 2026-09-04 — [Three refusals that belonged at the edge](docs/decisions/0199-three-refusals-that-belonged-at-the-edge.md)
- 2026-09-04 — [A format validator must not throw on a value it refuses](docs/decisions/0200-a-format-validator-must-not-throw-on-a-value-it-refuses.md)
- 2026-09-04 — [The amendment gate binds a lost race, and it binds both domains](docs/decisions/0201-the-amendment-gate-binds-a-lost-race-in-both-domains.md)
- 2026-09-04 — [What an aggregate Cell attendance view offers instead of buckets: nothing further](docs/decisions/0202-what-an-aggregate-cell-attendance-view-offers-instead-of-buckets.md)
- 2026-09-04 — [Two reporting rulings settled before Stage 5 code, and a third whose premise was wrong](docs/decisions/0203-two-reporting-rulings-settled-before-stage-five-code.md)
- 2026-09-04 — [The Cell roster read is guarded by `cell.view_subtree`, and an undated viewing read asks about now](docs/decisions/0204-the-cell-roster-read-is-guarded-by-cell-view-subtree.md)
- 2026-09-05 — [A report resolves the tree as of the end of its period, and attribution has three keys](docs/decisions/0205-a-report-resolves-the-tree-as-of-the-end-of-its-period.md)
- 2026-09-05 — [The owning module computes and `reporting` composes, and an unassigned person reports under their last leader](docs/decisions/0206-the-owning-module-computes-and-reporting-composes.md)
- 2026-09-05 — [A report's scope selector resolves as of the period being reported](docs/decisions/0207-a-reports-scope-selector-resolves-as-of-the-period.md)
- 2026-09-05 — [A report resolves the tree at the last millisecond of its period's final day](docs/decisions/0208-a-report-resolves-the-tree-at-the-last-millisecond-of-its-period.md)
- 2026-09-05 — [A report walks up past a leader who left the period, and the gap is surfaced rather than hidden](docs/decisions/0209-a-report-walks-up-past-a-leader-who-left-the-period.md)
- 2026-09-05 — [A report is computed in one read-only transaction at `REPEATABLE READ`](docs/decisions/0210-a-report-is-one-read-only-transaction-at-repeatable-read.md)
- 2026-09-05 — [A placement graph that is not functional refuses the figures it corrupts](docs/decisions/0212-a-placement-graph-that-is-not-functional-refuses-the-figures-it-corrupts.md)
- 2026-09-06 — [Interface debt is recorded, and a check derives what owes a screen](docs/decisions/0213-interface-debt-is-recorded-and-a-check-derives-what-owes-a-screen.md)
- 2026-09-06 — [A report's guard walks the as-of tree, outside the report's snapshot](docs/decisions/0214-a-reports-guard-walks-the-as-of-tree-outside-the-reports-snapshot.md)
- 2026-09-07 — [A Network grant of a dated capability covers no record](docs/decisions/0215-a-network-grant-of-a-dated-capability-covers-no-record.md)
- 2026-09-07 — [A report may not name a period that has not begun](docs/decisions/0216-a-report-may-not-name-a-period-that-has-not-begun.md)
- 2026-09-07 — [A Network grant resolves at the instant its target carries](docs/decisions/0217-a-network-grant-resolves-at-the-instant-its-target-carries.md)
- 2026-09-07 — [A report resolves at the period's final millisecond, open or closed](docs/decisions/0218-a-report-resolves-at-the-periods-final-millisecond-open-or-closed.md)
- 2026-09-07 — [A Network's reporting population is its membership](docs/decisions/0219-a-networks-reporting-population-is-its-membership.md)
- 2026-09-07 — [A `CELL` report scope selector resolves through the Cell's leader](docs/decisions/0220-a-cell-report-selector-resolves-through-the-cells-leader.md)
- 2026-09-08 — [A Cell figure's responsible leader is placed by the placement graph](docs/decisions/0221-a-cell-figures-responsible-leader-is-placed-by-the-placement-graph.md)
- 2026-09-08 — [A yearly report returns the months that have begun](docs/decisions/0222-a-yearly-report-returns-the-months-that-have-begun.md)
- 2026-09-09 — [A Cell meeting roster carries the marks it asks to be resubmitted](docs/decisions/0223-a-cell-meeting-roster-carries-the-marks-it-asks-to-be-resubmitted.md)
- 2026-09-09 — [DCC coverage over a month is obligations met over obligations owed](docs/decisions/0224-dcc-coverage-over-a-month-is-obligations-met-over-obligations-owed.md)
- 2026-09-09 — [A Cell that scheduled nothing stays in the coverage denominator](docs/decisions/0225-a-cell-that-scheduled-nothing-stays-in-the-coverage-denominator.md)
- 2026-09-09 — [A leader lists the Cells of their scope, with a filter for their own](docs/decisions/0226-a-leader-lists-the-cells-of-their-scope-with-a-filter-for-their-own.md)
- 2026-09-09 — [DCC events are listed by month, under the viewing capability](docs/decisions/0227-dcc-events-are-listed-by-month.md)
- 2026-09-09 — [A DCC coverage gap names the leaders who owe a record, within scope](docs/decisions/0228-a-dcc-coverage-gap-names-the-leaders-who-owe-a-record.md)
- 2026-09-09 — [A DCC event that has not happened owes nobody a record](docs/decisions/0229-a-dcc-event-that-has-not-happened-owes-nobody-a-record.md)
- 2026-09-09 — [A `NETWORK`-scoped DCC coverage figure narrows by membership at the event date](docs/decisions/0230-a-network-scoped-dcc-coverage-figure-narrows-at-the-event-date.md)
- 2026-09-10 — [A collection whose period dates its figures is a dated viewing read](docs/decisions/0231-a-collection-whose-period-dates-its-figures-is-a-dated-viewing-read.md)

### Open — awaiting a ruling

**Nothing blocks any stage. Sixty-seven things are unsettled, none of them blocking a stage.
They are listed at the end, so this section is the whole of what is open.**

*Two items have blocked Stage 5 and both are gone: the aggregate-view question left on 2026-09-04 as decision 0202, and the report-authorization question left on 2026-09-05 as decision 0207. **This sentence said "the one item" until the second one arrived and left**, which is the failure this section keeps recording, in the sentence whose whole job is the count. This
sentence carried a separate blocking count from the day the log was written; there is now
one number here rather than two, which is one fewer thing to keep in step.*

*Both numbers here are the same number and have to move together — this bolded sentence
and the italic below it. The batch that added the thirty-second bullet updated the italic
alone, because the instruction to recount lives only in the italic, and the bolded twin
is what a reader meets first. Anyone adding a bullet updates both.*

*Sixty-seven distinct items across sixty-seven bullets. **Four arrived on 2026-09-10** with `architecture-guardian`’s mandatory review of decision 0231, three of them Stop Conditions: whether an index row may link to a route that refuses it, which is the gap that ruling opens and is **not** the closed-Cell-fallback question it was first parked under; whether the actor’s own scope walk and its Network enumeration date with the collection, which is an authorization change the ruling deliberately did not make; whether a collection’s `ACTIVE`-state filter dates with its membership, on which §7 and the ruling now say different things; and which instant the category and schedule joins take, which are **inner** joins and therefore decide membership rather than display. The last two were each found by reproducing against the database rather than by reading. **None of the four is reachable until the code change lands, and the ruling ships without it**, which is why they are recorded here rather than fixed. **Two left on 2026-09-10 by owner ruling**, settled together as decision 0231: whether a collection whose period dates its figures is a dated viewing read, and which leader a period two leaders each held part of resolves through for a `CELL` selector. They were settled as one ruling because they are one shape asked of two routes, and settling either alone would have put that shape under two rules — which is the opposite of the interlock failure decision 0211 was withdrawn for, the interlock here being the reason to join them rather than an accident of drafting. The first had been recorded as owed to "the first screen that offers a month selector over this list"; **the screens block shipped that month selector and left the Stop Condition standing**, which is the "recorded as owed and never collected" pattern this log exists to catch, caught this time by counting the deferred items rather than by a review. **One arrived on 2026-09-09** with `architecture-guardian`'s mandatory review of the coverage slice, and it is a Stop Condition: which instant *within* a meeting date a Cell coverage subtree walk resolves at. The two halves of one change answered it two ways — the DCC half at `recordingInstant`, the earlier of now and the day's end, and the Cell half at the day's end unconditionally — and neither §20, §12 nor §13 states which. It is the second question that slice answered by itself, the first having been escalated and settled as decision 0230. **One arrived and left on 2026-09-09 without ever becoming a bullet**, settled by owner ruling as decision 0230: what a `NETWORK`-scoped DCC coverage figure narrows, and at which instant its membership resolves. It was found by reading §20 before writing the coverage slice rather than by a review, and it is recorded here because it was real enough to block a figure — §20's Network paragraph enumerates the person key and the responsible-leader key and omits coverage, which is the omission that produced decision 0221 one paragraph down. **The identical question for a Cell figure is not settled by it** and stays on the list below, the Cell domain having no person key to carry a narrowing across from. **A fourth arrived on 2026-09-09** with `architecture-guardian`'s review of the Cell meeting roster: which capability may read a meeting's recorded marks, on which §7 disagrees with itself and with the route. *This paragraph said "§22, the route and §7 each say something different" for as long as the detail bullet below carried the correction naming that attribution false — one rule, two homes, corrected in one. **No claim is made here about how often that happens**, because a first version of this marker ranked it above every other class in the log with nothing counted, in the paragraph whose subject is a claim that was wrong because nobody checked it.* It is the one arrival of the four that was **created** by a change rather than found beside one — until the roster carried marks, the divergence decided nothing. **Three arrived on 2026-09-09** with `architecture-guardian`'s reviews of the index and attention routes, and all three are Stop Conditions: whether an archived or merged disciple is a coverage obligation, which the first build answered by reading a **current** lifecycle row against a dated edge set and so moved a past Sunday's figure when somebody was archived today; and whether a collection whose period dates its figures is a dated viewing read, which §7's own text answers one way and the route's reachability answers the other. All three were raised by reproducing against the database rather than by reasoning, and the first was a live §3 violation rather than only an unsettled question. **The third arrived two review passes later**, from the batch fixing the second: suppressing a coverage line for a month that has not begun was a third answer to something §20 already settles, and the join change that accompanied it moved a row-uniqueness guarantee off a database constraint. **One arrived and left on 2026-09-09**, recorded with the first screens batch and settled before that record was reviewed, as decisions 0226, 0227 and 0228: there was no index route for Cells or for DCC events and §22 named neither, so every route that acts on one was unreachable from a screen. The coverage ledger could not see it — it asks whether every route has a screen, never whether every screen has its routes, which is decision 0213’s own failure one direction over. **Six left in all on 2026-09-09 by owner ruling**, the third being decision 0225: a Cell that scheduled no meetings reads `0 of 0`, is shown, and stays in an aggregate denominator contributing zero to both terms — because dropping it would make disappearance from the denominator a way of recording less and looking no worse, which is the incentive §12’s own argument for coverage exists to close., the second being decision 0224: DCC coverage over a month is the number of leader-events with a record over the number owed, summed across the month’s events, with both terms reported and never divided. It is Section 20’s "attributes by the obligation rather than by the record" made arithmetic, and it inherits Section 20’s own instant, so a leader assigned mid-month owes records from that date and not before it. **One arrived and left on 2026-09-09**, recorded with the screens block and settled as decision 0223 before that record was reviewed: a Cell meeting correction must resubmit the whole roster and nothing let a client read the marks it was resubmitting, so the screen would have overwritten them blank. It was the first item here to block a **screen** rather than a figure. The ruling is decision 0194’s reason applied to the domain that asks for more, and the shape is the Cell’s rather than a copy of the DCC line: no per-person version, because a Cell submission compares the meeting’s. **Two arrived on 2026-09-08** with the coverage work, and both block the aggregate figure while leaving the per-Cell one alone: what a coverage line reads when its **denominator** is zero and whether such a Cell enters an aggregate denominator, raised by `architecture-guardian` where that answer had been written into a docblock; and, with the reading for the DCC coverage slice, before any code: how that figure’s per-event definition aggregates over a month, which §9 leaves unstated while asking for "a single line". It is the first item here to **block a slice** — it blocks DCC coverage and no stage, Stage 5’s exit criterion being met — and Cell coverage is unaffected, so the other half of that work proceeds. **Two arrived on 2026-09-08** with `architecture-guardian`'s review of the fix batch for decisions 0221 and 0222, and both are Stop Conditions: where a responsible leader who held an assignment **before** the period, none within it, and whom no edge of the placement graph names as leader, is placed — stated as the graph’s own condition after three English paraphrases each failed, and reproduced against the database as a root's Cell figure of 0 against a Whole Church 1, and the exact gap 0221's second residual was written to close and does not — and which month's classification an in-progress year carries, which that fix batch answered "December's value" in **two** files while the ruling it was fixing forbids that answer. Both arrived from a pass over a batch of fixes rather than over new writing, which is the measured pattern this file records and the reason that second pass is mandatory. **Two left on 2026-09-08**, both settled by the owner ruling on escalated Stop Conditions rather than by anything being retired through reasoning. The second is decision 0222: a yearly report returns the months of the selected year that have begun and omits the rest, naming the months it covers — because omitting them silently would reproduce, one unit up, the exact failure decision 0216 refuses, a report that comes back looking whole and is not. **It also names something nobody had asked about**: a year's classification is not the sum of its months', since the ladder is a lifetime count truncated at each month's end, so a yearly view that added twelve monthly reports would double-count and would reconcile while doing it. That is recorded in §20 rather than settled. **The first** is decision 0221, the ruling the coverage slice had to have before it was written: which graph places a Cell figure's **responsible leader** in a subtree. Section 20's placement graph, over the period — the same walk the person key uses, which is what the code already did. **The sentence that made it a Stop Condition is the one Section 20 uses to enumerate its own instants**: it named the period's end for the person key and each meeting date for coverage, and omitted the middle key entirely, from the day it was written. Coverage adds a *third* key, so building that on an unstated second one is how a figure comes to reconcile and be wrong. It also names a **second residual** Section 20 had no sentence for, about the meeting's leader rather than the counted person. **Three arrived on 2026-09-08** with `architecture-guardian`'s review of decision 0220, and all three are Stop Conditions: which graph places a Cell figure's **responsible leader** in a subtree, which §20 states for the person key and not for its second key; which leader a period two leaders each held part of resolves through, which is the handover mirror of the closure month 0220 turns on; and what a `NETWORK`-scoped Cell figure narrows. **The third was already true of the code and was asserted by three shipped artefacts to be recorded here, while it was not** — two docblocks and a commit message, the seventh "recorded as open" falsehood this log counts and the first committed on a branch whose own review brief named the pattern. Writing the bullet is what made the claim true rather than the claim being withdrawn. **One left on 2026-09-07**, settled as decision 0220 and the ruling the first Cell-scoped reporting route owed: what a `CELL` report scope selector resolves through, and whether the closed-Cell fallback reaches it. It resolves through the Cell's leader at the period's instant, falling back to the Cell's last leader — which is §7's base Cell bullet applied to a target that bullet had never been read against, the selector bullet having only ever supplied an instant. **The bullet's own framing is what the ruling had to discard.** It recorded the question as narrow because "a report for a month after closure is empty either way", and the deciding case is the month a closure falls *in*: §12 says such a month is reportable and it holds real recorded attendance, so resolving through nobody would withhold it from the leader who recorded every row of it. **Two more arrived on 2026-09-07** with the review of the batch answering that review, and both are Stop Conditions raised by one fixture: which leaders a Network's drill-down offers, and whether a Person outside the pastoral structure may acquire disciples. They arrived together because the fixture decision 0219 rests on had to be rebuilt on the second in order to be reachable at all — §9 refuses a DCC record to somebody with no assignment, so §20's residual, which that ruling first argued from, cannot appear in a DCC population. **One arrived on 2026-09-07** with `architecture-guardian`'s review of decisions 0218 and 0219, and it is a Stop Condition: whether two `network_assignments` rows may be in force at one instant, which decision 0219 turned from a curiosity into the thing §20's new Men's + Women's identity rests on. It arrived because a ruling made an identity exact enough to be broken, which is the useful direction for a review finding to run. **One left the same day**, settled as decision 0218 and the third owner ruling of the day: which instant an **open** period's report scope selector resolves at. The period's final millisecond, open or closed — and the answer was that Section 20 had borrowed the instant from Section 16 along with a gloss that belongs to a current-state metric rather than to an instant, so there was one rule wearing a clause it had picked up in transit. **It is here rather than in the ruling because of how it was found**: the claim was asserted four times on one branch by people settling nothing — in §7, in decision 0217, in decision 0216's fourth supporting argument, and in a `hierarchy` docblock citing an already-closed question — and three of those were caught by `architecture-guardian` while a sweep reported as clean missed the fourth. *This said "twice in prose, once in a ruling, once in a docblock", which reconciles with decision 0218's enumeration of the same four only if 0216's argument is counted as prose rather than as a ruling. Both were written in one commit; the four are now named rather than tallied by kind.* A sentence that keeps being written by nobody deciding anything is a question overdue. **One arrived on 2026-09-07** with `architecture-guardian`'s review of the two rulings below, and it is a Stop Condition: whether a yearly report may cover a year still in progress, which decision 0216 made unanswerable for the months inside it without saying which unit its rule binds. **Three left the same day**, settled as decisions 0216 and 0217, both by the owner ruling on an escalated Stop Condition rather than by anything being retired through reasoning. Decision 0217 took two of the three together, because the second existed only as a consequence of the first having been answered for one target kind: a `NETWORK` grant resolves through `network_as_of` at the instant its target carries, which binds all three dated capabilities and supersedes the refusal decision 0215 issued earlier the same day, neither having reached `main`. **That refusal rested on a claim this repository refutes in one grep** — that no dated Network resolution existed — and 0215's own decision file said the function existed while §7 and the service docblock said it did not. The ruling was still right to refuse while no rule existed, which is why 0215 stands and is not withdrawn. The remaining one, settled as decision 0216 and the first of the two Stop Conditions the route raised to be answered by the owner rather than deferred: a report may not name a period that has not begun, refused against the period's start instant read from the database. It retires a bullet recorded the same day it was written, which is the shortest-lived Stop Condition on this list; what made it worth a ruling rather than a fix is that the route's own suite had been exercising the future case for four review passes with nothing able to fail on it. **One left and three arrived on 2026-09-07**, and the first of those was recorded when the count did not move, because a still count hides a swap. The first reporting route landed, so the dated upward walk stopped being a work item; and `architecture-guardian` raised whether a report scope selector may name a Person who does not exist, `NIL_UUID` included. That pass also found the `NETWORK` bullet asserting the case was unreachable on the day the route reached it, and decision 0215 now settles what that route does without settling the question. **The second pass added two more, both Stop Conditions**: whether a report may name a period that has not happened — the route's own suite reports on a month three weeks ahead of the day it was written, and 0215's first refusal message said "past period" about it — and whether 0215's answer binds the other two dated capabilities or only this target kind, which is a narrowing nobody ruled on. **Three arrived on 2026-09-06** with `architecture-guardian`'s review of decision 0214, two of them Stop Conditions: how a `NETWORK`-scoped grant resolves for a capability §7 makes dated, which 0214 supplies nothing for and which is escalated rather than decided; which instant an **open** period's selector resolves at, where §7 and §20 say "now" and the code says the period's final millisecond, agreeing only because no writer produces a future `started_at`; and whether a leader inside an actor's own total but outside their reach should be requestable, which is 0214's cost stated as a question. Both were escalated rather than answered. *The sentence above said "one is a Stop Condition" and this one said two, three clauses apart, from the day they were written.* **Two left on 2026-09-06**, settled together as decision 0214 with the first reporting route's guard: whether a report's authorization walk runs inside the report's own snapshot — it does not, which keeps §24's third clause true by design rather than by there being no route — and which graph authorizes a leader-scoped report, answered as the tree in force at the period's end rather than §20's placement graph, on the ground that §20 requires the condition widening that graph to be surfaced as a gap needing repair. **Three bullets were owed by that one route and a fourth was a facing of one of them**, which is why they were settled as a single ruling rather than three: the per-person half was narrowed and stays below, and the dated upward walk stayed below as a work item until the route landed on 2026-09-07 and retired it. *That is the opposite of decision 0211's mistake rather than a repeat of it — 0211 was withdrawn for being drafted early and separately, and these were settled with the service, the guard and the walk in hand — **not with the route, which does not exist and is what 0214 makes buildable**. This clause said "with the route's code in hand" for one commit after the commit that claimed to have removed that phrase everywhere, in the line that same commit rewrote.* **One arrived on 2026-09-05** with `architecture-guardian`'s review of slice 3b, and it is the seam decision 0211's withdrawal left open rather than closed: whether a report's authorization walk runs inside the report's own snapshot. It is recorded now because the next slice is the route that owes it, and because it is the only thing that pass named as making the route unsafe to add. The count before it was fifty-two — one more than `main` carried, and two of the fifty-one are different items. **One left and two arrived on 2026-09-05.** The second arrival came from the review of this batch's own fix commits: decision 0212's refusal reaches an overlap in force at the period's end and not one that opens and closes inside it, which the ruling's stated trigger was wider than. **One left**, settled before slice 3b as decision 0210: whether a report must be computed from a single snapshot. It is one `READ ONLY` transaction at `REPEATABLE READ`, a **per-transaction** override and never a change to the default, since §24 names three lock-then-decide mechanisms that would silently decide on a pre-lock snapshot if it moved. *That refuted this session's own first recommendation, which was a broader pin, and it was caught because the ruling was written and checked against §24 before any code existed.* Decision 0212 was settled with it and retires nothing: it fixes what a report does when the placement graph is not functional, while the §5 question about overlap stays open below. **One arrived**, and it arrived by a ruling being withdrawn: whether §20's placement graph may serve as a visibility grant. A third ruling, 0211, was drafted in the same batch to settle which graph authorizes a leader-scoped report, and was withdrawn before review — this file already said that walk is built with the route rather than in advance, and drafting it early coupled it to 0210 in a way that produced most of the findings against both. **Three rulings drafted, two kept, and the batch is the lesson**: interlocking rulings written together fail at the interlock. **Three arrived on 2026-09-05** with the second review of the placement graph, none reachable: the blast radius of §20's cycle refusal and whether such a cycle can be repaired at all, where a person with a pre-period assignment who leads nobody is placed, and whether §20's archived-leader attention list keys on lifecycle or on the condition it exists for. **One left on 2026-09-05**, settled as decision 0209 and the only thing that had blocked a leader-scoped figure: the chain continues past a leader who held no assignment within the period, resolving them from their last one whenever it was, and a person whose leader is archived goes on an attention list so the fix does not hide the gap. The cycle half needed no ruling — §20 already says a detected cycle refuses, and what was wrong is that detection was scoped to the component the walk reached. **Two arrived on 2026-09-05** with `architecture-guardian`'s review of the placement graph — what a leader-scoped report does with a graph component it cannot reach, which **left the same day** as decision 0209, and which graph authorizes a leader-scoped report, which has not — a ruling settling it was drafted and withdrawn on 2026-09-05, and the bullet carries why. *This clause was edited to say it had left, and edited back when the ruling was withdrawn the same day. Both edits were right when made, which is the argument for recording arrivals and departures in one paragraph rather than trusting a bullet list to stay current.* *The first was described here as blocking for as long as it was, and the clause saying so outlived it by one commit.* **Three arrived on 2026-09-05** with `architecture-guardian`'s review of the first reporting slice, two of them Stop Conditions: what a DCC report does when N is zero and whether a gap month is reportable, whether a report must be computed from a single snapshot, and where a merged identity is resolved for reporting. The snapshot one is the useful one — that slice's own instance was a real defect, two figures read on two pooled connections while two docblocks claimed the identity held "by construction" — and it **left on 2026-09-05** as decision 0210, before the first report that could not hold the identity in one statement. **One left on 2026-09-05**, settled as decision 0208 with the first reporting query that owed it: a report resolves the tree at the last millisecond of its period's final day, which is `endOfManilaDay` — the helper §13 already uses at the closure boundary, adopted rather than a second answer being derived. **Two arrived on 2026-09-05** with `architecture-guardian`'s review of the dated subtree walk: whether two `pastoral_assignments` rows for one Person may overlap historically — a Stop Condition, unreachable today and held by an application-layer floor rather than by a constraint — and the dated *upward* walk §7's report authorization needs, which is the security-sensitive half decision 0207 did not supply. **One arrived on 2026-09-05** with the review of decision 0207, and is narrow: what a `CELL`-valued report scope selector resolves through, and whether the closed-Cell fallback reaches it. **One left on 2026-09-05**, settled as decision 0207 and the last thing blocking Stage 5's first query: a report's scope selector resolves as of the period being reported, so a leader may read and drill into October's figures for somebody who left their subtree in November. §7 fixed datedness to the capability and named `reports.view_subtree` there; the one target with no instant was the selector, and that is what the ruling supplies. It moves no write — the current-leader rule that stops privilege being reclaimed through a date field is untouched. **Two left on 2026-09-05**, both settled as decision 0206 and both escalated to the owner rather than invented, which is what the Stop Conditions are for: `reporting` roots no query in another module's tables and composes what the owning modules compute, leaving §2 unamended; and a person with no open assignment at the period's end is placed by the last one they held within the period, so a drill-down still sums at every level. **Three arrived on 2026-09-05** with `architecture-guardian`'s review of decision 0205, and two of them are Stop Conditions rather than passing items: where a person with no open pastoral assignment lands in a report, whether `reporting` may root a query in any table beyond the two it owns, and whether a report asking about a past month is authorized dated or undated — **withdrawn, restored, and then settled**, all on 2026-09-05. The withdrawal rested on the target's kind while §7 keys datedness to the capability; `architecture-guardian` refuted it, it was escalated rather than re-decided, and decision 0207 settled it dated. **One arrived on 2026-09-05** with decision 0205, which settled that a report walks the tree as of the end of its period and declined to fix the instant within that day — the first reporting query owes that, and §13 already names an instant at the closure boundary for the same reason. **One left on 2026-09-04**, settled as decision 0204 before any Stage 5 code: which capability guards a Cell roster read. `GET /api/v1/cells/{id}/members` now carries `cell.view_subtree`, so roster visibility is grantable `read_only` — which §7's CHECK constraint had made not merely ineffective but uninsertable while a management capability guarded it. That ruling also narrowed a tripwire it tripped: `capability-scope-resolution.spec.ts` refused *any* Cell-targeted viewing route on the ground that the dated resolution does not exist, and §7 owes that resolution to a read asking about a **past period** rather than to every route in the viewing class. **Three left and one was narrowed on 2026-09-04**, settled as decisions 0202 and 0203 before any Stage 5 code: what an aggregate Cell attendance view offers instead of buckets — the item that had blocked Stage 5 since Stage 0 — what category a closed Cell carries for a report inside the month it closed, and whether an Admin amendment may create a record for a person who had none. The narrowed one is the Cell roster read, whose premise turned out to be false: `cell.view_subtree` already exists in §7's list, is named among its Read capabilities, and guards no route. **One left on 2026-09-04**, settled as decision 0200: whether a format-constrained field that is nonetheless stored owes §22's storability rule. It does, one step further along — a validator that constrains a format must be able to evaluate every value it is given and must refuse rather than throw. It was the only item on this list reachable by somebody with no account, and the fix could not be decorator ordering: class-validator runs every validator on a property rather than stopping at the first refusal, so the guard had to sit inside the same validator. **Two left and one narrowed on 2026-09-04**, settled as decision 0199 before Stage 5: an explicit null on a DCC line now means what omitting the key means, an element of a typed array must be an object, and `facilitated_by` must name a Person who exists — that last one narrowed rather than retired, because what the field may name *beyond* existing is what made it a ruling. The array item left in a worse state than it was recorded in: it had been written as "not blocking, no malformed body is admitted", and the identical body that answered 409 on the Cell roster answered **500** on the DCC checklist, which the mutation found and neither reading of the route had. **Two arrived on 2026-09-04** with the review of decision 0198, and one of them is the only item on this list reachable by somebody with no account: every `@IsEmail` field answers 500 on an unpaired surrogate, on two unauthenticated routes, because `validator`'s own `isEmail` throws before any decorator runs. The other is how a refusal inside a nested object names the field that broke, which §22 was made to state rather than overstate. **One left on 2026-09-04**, settled as decision 0198 before Stage 5: which free-text fields outside the Cell meeting submit route must be storable. It closed in the wider of the two directions the bullet framed — section 22 now states the rule and it binds every field that accepts arbitrary text, twenty-two of them — because the narrow answer was the one that had already failed twice: the scope lived in a docblock, so being missed was undetectable. The check that replaces the docblock derives its own field list and found one nobody would have listed, `SearchPeopleDto.q`, which is a search term rather than a stored value and reaches the database as a query parameter. **One arrived on 2026-09-04** with the eleventh review pass, which reviewed the tenth pass's fix batch and found the same one-rule-one-path shape for the fourth consecutive round — this time inside the new file written to close it, whose docblock claimed the rule was "applied to every free-text field on a route" while one field of that route's own DTO was undecorated and answering 500: which free-text fields outside this route must be storable. That pass also established that the lone-surrogate half of the rule does not 500 at all but is silently stored as U+FFFD, which is what makes the uncovered fields worth a ruling rather than a shrug. **One arrived on 2026-09-04** with the tenth review pass, which reviewed the ninth pass's own fix batch and found that batch had introduced a behavioural regression, reintroduced the one-rule-one-path shape the ruling it was writing exists to condemn, and written eight false claims about itself: what a `correction_reason` means on a first submission and on a transition to `NOT_HELD`, where it is accepted and discarded. It is pre-existing on `main`; what the fix batch changed is that the field is now honoured on two of the four paths rather than none, which makes the asymmetry visible. **Two arrived on 2026-09-04** with the ninth review pass, which was narrowed to the same two classes the eighth audited and asked whether the fixes for them held: whether the DCC submit route should normalise nulls at its door as the Cell route now does, and whether an array-valued element inside `attendance` should be refused at the edge. The first is the more useful of the two, and is why that pass answered its Class A question "no" — the null class was closed at one door and the sibling door had never been checked, which is the shape every fix batch on this project has taken. Both are pre-existing on `main`, neither blocks. **One arrived on 2026-09-04** with the eighth review of the transitions slice, which audited two defect *classes* rather than sweeping again: whether an unknown `facilitated_by` should be refused rather than answering 500. It is pre-existing on `main` and is recorded so it is not rediscovered as a sixth constraint-driven 500 on that route. **One arrived on 2026-09-04** with the transitions slice, raised by `architecture-guardian` reproducing seven behavioural defects on it: what bounds a reschedule's actual date, beyond the one refusal §13 states and the one the fix chose. **Two arrived on 2026-09-04** with decision 0195, and both are things that ruling deliberately declined to settle rather than questions it raised: how a meeting status recorded in error is corrected, and whether a reschedule may name a date whose own month has shut. Each is refused conservatively today, so neither blocks. **Two more arrived on 2026-09-03** with the first-submission race, both raised by `architecture-guardian` reproducing them rather than reasoning about them, and both about what a lost race answers: whether §22's outcomes cover a disagreement about a meeting's *status*, which has no line to compare, and whether §7's amendment gate binds a submission that differs from what is stored while having amended nothing. Each is implemented in the conservative direction — the loser is answered what the identical body gets sequentially — so neither blocks, and what is open is whether that answer is right rather than merely safe. **Two arrived and left on 2026-09-03**, which this paragraph no longer ranks against every other same-day item, because it twice ranked it wrong. *It said "the second and third". The fix said "the fifth and sixth", counting the one of 2026-09-01 and the three of 2026-09-02 and missing the two of 2026-08-22 recorded three paragraphs below. The answer was seventh and eighth, so both were wrong — a sentence whose whole content is a number, wrong in the commit that wrote it and again in the commit that corrected it. No magnitude is given for either error: the first correction stated one, and it was computed against the second wrong answer rather than the right one, which is a third arithmetic claim in the same sentence and was itself wrong.* The ordinal is dropped rather than corrected a third time: it ranges over the entire history of this list, it is checkable only by auditing every retired entry, and nothing depends on it. **The bullet count above is different and stays** — it is one command, it is stated twice, and the instruction is to run the command rather than to remember the answer. Both were about DCC rather than the Cell path the pass that raised them was reviewing, and they arrived at all because the fix batch for a §7 narrowing asserted the Cell path's safety ground over the DCC path as well — the third narrowing of one sentence, each introduced by the fix for the last. Settled as decisions 0193 and 0194: §7's contents-ordering rule **binds** DCC, so the correction-reason refusal moved behind the on-behalf check and a 409/403 record-existence pair went with it; and §7's accepted disclosure was always a Cell argument, while the DCC roster publishes per-person figures by design, because a leader marking a checklist must see who is already marked. They were recorded as one question and settled as two — the DCC roster shows the actor's checklist and the oracle concerned somebody off it, so the ordering was owed however the disclosure went. That is the opposite of the coupling the bullets claimed, and it is written here because "answering either alone settles the other by accident" was wrong in the direction that would have left a defect standing. **Three arrived on 2026-09-02** with the dated Cell-meeting scope, and **all three left the same day**, settled before slice 2c as decisions 0185, 0186 and 0187. One was found by its own suite failing on its first run: which leader a meeting belongs to when the Cell changed hands on the meeting's own day, which the date comparison section 13 requires at the closure boundary cannot decide — and which decides reporting attribution rather than only a scope answer, the first version of that bullet having said otherwise on a claim about the other two paths that was false of both. Settled as the earlier-starting row, on the one ground that is not a frequency claim: the other answer made a meeting's permanent attribution depend on whether the handover had been recorded when somebody filed it. **One item arrived with the fix batch for those three** and is below: §7 answers twice, and not identically, whether a closed Cell's last-leader fallback survives for a capability that resolves as a write. The second was found by architecture-guardian and **left on 2026-09-02**: whether a meeting-scoped roster read follows section 7's write rule or section 13's read rule, which disagreed on an ACTIVE Cell and which the code had settled in a port docblock rather than in the specification. Settled as decision 0186, and by neither of the two readings the bullet framed it between — the capability decides and the HTTP method does not, so a recording capability resolves as a write whether its route reads or writes. Behaviour is unchanged; what it removes is a rule living in a docblock. **The third left the same day it arrived**: what an API does with a date-only value that is well-shaped and is not a day, settled as decision 0185 before slice 2c, which adds more date-carrying routes than anything built so far. It closed with it the live defect that bullet had named — a Cell closure written effective on 2026-03-02 because `2026-02-30` was normalised rather than refused, with the response and the audit entry both reporting the invented day back. **One arrived and left on 2026-09-01**, which is the shortest any item on this list has lived: whether a Cell meets on the day it was created, found by a mutation on the Cell meetings listing rather than by a reviewer, and settled as decision 0184 before the recording slice that turns it from a display choice into a refusal. It counts, and the bound is a Manila date at both ends — the closing edge because section 13 requires it, the opening edge because a bound granular one way at one end and the other way at the other is two rules wearing one name. **Three others left the same day**, settled together before any Cell meetings code as decisions 0181, 0182 and 0183, and one arrived with them. Whether a port a module cannot bind for itself is optional-and-refuse or mandatory: optional and refusing, with the module-graph test asserting the binding resolves — which is what makes the cost the bullet named against optional disappear rather than be accepted. *Settled without its own trigger firing, and the first version of that ruling invented one: no second port is being declared, because `attendance → cells` is not a cycle and §2's ordinary service-interface rule covers it.* What a closed month's Admin amendment does: a flag on the two submit routes rather than a third endpoint, and **the half that looked blocked on Stage 5 was not** — section 20 says four lines below the clause that stored figures are keyed to a version of their source records rather than enumerated by the write paths that dirty them, so the amendment owes nothing and the obligation was always the snapshot's. That is the second time on this project a clause was called undefined while its own section defined it. And whether an attendance record may be closed with no replacement: the row names itself, and section 13 now states that as the idiom rather than as a workaround. The one that arrived is whether such an amendment may **create** a record for a person who had none, which moves a closed month's numerator while its coverage denominator is frozen. One arrived on 2026-08-31 with the `architecture-guardian` review of the DCC recording slice: whether a person may record their own DCC attendance, which `dcc.submit_on_behalf` permits today and no section addresses. One left on 2026-08-31 with the DCC roster, which is the fourth cursor the retired bullet said would settle it: two of the four page by the identical key and now share one pair in `common/roster-cursor.ts`, and the two whose keys differ keep their own — so it closed by being answered rather than by being dropped. One arrived on 2026-08-31 with the audit-target ruling, raised by the second architecture-guardian pass on it: what period a read of the audit log is asking about, which Section 7 leaves one layer in from a phrase it does define, and which three drafts of that ruling got wrong three different ways. That cursor item had arrived by being **found already claimed** — `leadership-request-cursor.ts` said the question was "recorded as open" and it was not, the sixth false "recorded as" claim on this project — and it is noted here because the claim outlived the thing it was false about by one day. Three left on 2026-08-31, settled together before any Stage 4 code and recorded above: which target a Cell leadership audit entry carries, answered as the Cell; what error code a refusal answers when a value read before a lock differs under it, answered by a question rather than by a list — could this same body, resubmitted unchanged, succeed? — which places six of the seven refusals on `RESOURCE_BUSY` and leaves the seventh an `INVARIANT_VIOLATION`; and what a collection endpoint does with a cursor it cannot resolve, answered by refusing it. (Seven, rather than the five the retired bullet claimed or the six a method-count gives: decision 0158 fixes the unit as one error-producing branch, this branch having counted it three ways before noticing. That aside is parenthesised because it interrupted the list of three above when it was not.) A fourth was narrowed rather than closed the same day and is still below: the instance count is settled at one, and the skew bound it was carrying is now owed by whatever change introduces a second instance. One arrived on 2026-08-30 with the `DateStyle` pin and was raised by the review of it: whether the isolation level should be pinned the same way, the ruling having assumed the two differed in kind on a belief that turned out false. One left on 2026-08-30 with the `DateStyle` pin, which settled it in the direction of both the answers that could fail: the pool pins it and the application refuses to start unless the pin took effect. One left on 2026-08-30 with the backdate floor's Cell terms, which settled what it asked: a closed Cell leadership bounds on its `ended_at`, and a closed Cell membership on its `started_at` extended to the last leadership start it spans. One arrived with the Network-change precondition — whether a port a module cannot bind for itself should be optional-and-refuse or mandatory — and left on 2026-09-01 with decision 0181, though **not** for the reason its own instruction gave. That instruction said to settle it before a second port is declared, and no second port is being declared: the ruling's first version claimed Cell meetings forces one, which the module graph refutes in a single command — `attendance → cells` is not a cycle, `CellsModule` already exports `CellsReadService`, and §2's ordinary cross-module rule covers it. What makes the ruling due instead is that the two ports already in the tree are injected optionally and **nothing asserts either is bound**, which is a gap rather than a hypothetical. Two arrived with the Cell leadership approval slice, both escalated by its first architecture-guardian pass: which kind of target a leadership audit entry carries, which left on 2026-08-31, and whether a request's `cell_id` should be frozen while it is PENDING, which has not. A third was *widened* rather than added by that slice's third pass — the error code a stale-premise refusal answers, which left on 2026-08-31 — and a batch that claimed to widen it duplicated it instead, which is why this sentence is one a reader should not trust without recounting. Three arrived with the leadership
request slice — the third being whether the application should pin the database session's
`DateStyle`, raised by the third review pass. The first two are: how a requester sees the outcome of a request they submitted, which
section 19 requires and section 7 names no capability for, and whether section 7 should
refuse a grant of `cell.request_leadership` wider than `SUBTREE_EXCL_SELF`. Both are
named here because the sentence whose job is the count named one of them and left the
other to be found by counting. Three arrived with the closure
endpoint's reviews — whether a Cell roster read deserves a capability of its own, which
left on 2026-09-04 by having its premise refuted and then settled (decisions 0203 and
0204: the capability already existed), what a
collection endpoint does with a cursor it cannot resolve, which left on 2026-08-31, and
whether a name has a
maximum length — and two left on 2026-08-30, both settled before the leadership-request
endpoints were written: a requester may decline their own request, and a decision is
final. A further
two left on 2026-08-29. Those two are the
pair that had come back: the closure effective-date floor and the cross-class lock
ordering, each written three times in prose and refuted three times, both settled by
the closure endpoint running the database rather than by a fourth formulation. The
outlive-closure question left with the floor, having been the thing the floor was
blocked behind. It said "twenty" from the day it was written through six commits that
added fourteen bullets without touching it, which is the miscount this log keeps
recording, committed against the sentence whose only job is the number. Anyone adding
a bullet updates it here, and counts rather than remembers:
`awk '/^### Open — awaiting a ruling/,0' CLAUDE.md | grep -c '^- \*\*'`.*

The two Stage 3 questions that stood here — how a Cell changes hands, and what reversing a closure does — were settled the same day by the ruling above: a handover goes through request-and-approve, and a closure is never reversed. Nothing in Stage 3 is now blocked.

The refresh-token question that stood here since 2026-08-27 — what a client does
with a presentation whose outcome is unknown — left this list the same week, settled
by the ruling above: a rotated token presented again, whose replacement was never
used, is a retry rather than reuse, inside a bounded window. The client behaviour it
describes stops being interim with it.

Nine items that stood here on 2026-08-22 were settled that day and are recorded
above. Seven were Stop Conditions for Stage 2, and the last two were opened and
closed the same day by `architecture-guardian` passes.

The `audit_log.action` vocabulary left this list on 2026-08-23, settled by the
ruling above: the convention is `<noun>.<past-tense verb>`, and section 21's list
stays open.

**What an aggregate Cell attendance view offers in place of buckets left this list on 2026-09-04**, settled as decision 0202 and written into `SKILL.md` §12. It was the only item marked as blocking Stage 5 **at the time**, and a second arrived and left on 2026-09-05 as decision 0207. **Nothing blocks Stage 5 now.** The answer is that nothing replaces the buckets: unique people, classification and coverage are the whole of an aggregate view, and coverage leads. The ground is that the bucket denominator is self-reported, so every candidate replacement inherits the incentive — including normalising each person against their own Cell's N, which fixes the arithmetic and leaves a Cell recording one meeting still producing the most `Completed` people. §12's own closing constraint is about the incentive rather than the arithmetic. Coverage is exempt because its denominator is derived from the schedule rather than submitted. *Half of what this bullet framed was never open: §12 already ruled that bucket views exist at Cell scope only, and a draft of the ruling presented that as its finding.*

Two related questions have defined behaviour and are recorded in `SKILL.md` §12 as fairness questions rather than Stop Conditions: whether a leader should see someone who attended and has since left, and whether a mid-month joiner measured against the whole month is acceptable. An implementer follows the stated rules and does not stop on either.

**Unsettled, and none of it blocks a stage or a screen.** **Seventeen** of these are labelled Stop Conditions where they sit; the rest are not. *It said "three" while **one** bullet carried the label, and has been corrected on every day since that bullets were labelled — including one commit after the bold number it explains. **Three successive attempts to narrate its own history were each wrong about that history**, so the history is not narrated here any more: the number being right now is not evidence the sentence is maintained, and the command below is — a count in the sentence whose only job is the count, which is the failure this whole section keeps recording. Whether two labels were dropped or the number was never right is not reconstructible, so it is counted rather than restored: `awk '/^### Open — awaiting a ruling/,0' CLAUDE.md | grep '^- \*\*' | grep -c 'Stop Condition'`.* An implementer proceeds and settles them in passing; they are listed here because a reader looking for what is open should not have to find it inside the body of a ruling.

- **What period a read of the audit log is asking about.** Section 7 resolves an audit entry through its target and resolves a Cell "through the Cell's leader as of the period being viewed, falling back to its last leader where the Cell is closed" — and it *does* define that phrase, under **An effective date does not move the scope decision**: the period a request under a **viewing** capability is asking about, everything else being "acted on now". *It read "the period a read is asking about" until 2026-09-02, and this bullet quoted those words as §7's for one commit after they stopped being. `audit.view` is one of the three capabilities §7 now names, so the phrase reaches this log by capability rather than by method — which changes nothing about the question below.* What is open is one layer in from there. A report asks about a month; a read of this log might be a single entry, which is an **instant**, or a filtered range, which is a range. Under the first, a Cell-targeted leadership entry resolves through whoever led the Cell when it was written — for `cell_leadership.opened` that is the person the entry names, so it never diverges from a person target. Under the second it resolves through whoever led the Cell across the range asked for. The two disagree about who may read a past leadership entry. Not blocking, and the distinction is worth keeping: **writing** an entry is identical under both, so the 2026-08-31 target ruling stands either way and Stage 4 may write freely; what the answer binds is the first route that **reads** the log, and `audit.view` has none. Nor is the code an answer yet — `CellsReadService.leaderForScopeWithin` resolves the latest leadership row and every Cell-scoped path goes through it, but `cell-scope.port.ts` says in terms that this is "the undated case rather than a claim that dates do not matter". *It also said a past-period read "will need a dated variant", and the branch of 2026-09-02 built one -- so that half of the quotation now survives in that file only inside the retraction recording that it was left standing one commit too long. **The trigger this bullet names has not fired, and for two days this paragraph said it had.** Decision 0186 settles that a meeting's scope resolution follows its capability rather than its HTTP method, so `leaderForMeetingScope` is a dated resolution serving a **recording** capability and is not a read in section 7's sense. **What settles this is the first `audit.view` route**, which is the event this bullet already names above — five sentences above, counted, and a version of this sentence said two — and which no other event can stand in for: a Stage 5 aggregate report resolves no Cell target at all, so it cannot decide how a Cell-targeted audit entry is dated. A previous version said the trigger was "Stage 5's reporting", which is the overreach four other places on this branch had removed, surviving in the one paragraph that owns the question.* Settle it with the first `audit.view` route, and note that the phrase governs every Cell-resolved target rather than the audit log alone. Raised by `architecture-guardian` after two drafts of that ruling asserted a divergence mechanism and a third called Section 7 silent.
- **Whether a person may record their own DCC attendance.** Section 9 makes a person's attendance their *direct pastoral leader's* obligation, so a person is never on their own checklist — the submitter walk starts at their leader. But `dcc.submit_on_behalf` at `OWN_SUBTREE` covers the actor themselves (`scopes.ts`: "The actor's pastoral subtree, including the actor"), so a line naming the actor is admitted today, written with their leader as responsible leader and themselves as `recorded_by`, and it completes their leader's coverage. Section 14 permits recording "on behalf of a downline leader within their pastoral subtree" and says nothing about oneself; section 5 has an explicit prohibition on acting on oneself for pastoral assignment and section 9 has no counterpart. Whether that is right is a pastoral question rather than a technical one: section 9 rests recording on "a leader knowing who was in the room", and self-reporting is a different basis. Not blocking — the record is honest either way and nothing is corrupted — and decision 0172 deliberately declined to settle what an actor may submit for beyond their own roster. Raised by `architecture-guardian` on the DCC recording slice.
- **Whether a name has a maximum length.** Section 3 says a name may hold any character and is silent on how many. `persons.first_name`, `middle_name` and `last_name` are bare `text` with only not-blank checks; the create and edit DTOs bound each at 100 UTF-16 units, and the tree import — which writes through the services rather than through a DTO — bounds nothing. So 100 is an implementation choice on two paths rather than a property of the data. It surfaced because a pagination cursor carries two names, so its length is unbounded exactly where the name's is: no finite bound on `CURSOR_MAX_LENGTH` is provable, and the constant is a request-size guard rather than a derivation, which its docblock now says. Not blocking — the guard sits about four times clear of anything a validated path produces, and far beyond what the spine import carries. What would settle it is a stated maximum in section 3, enforced as a `CHECK` constraint (the Definition of Done: an invariant expressible as a constraint exists as one) and applied to the import; that is a domain rule with a migration attached, which is why it is not decided in a pagination file. Whoever settles it should also say whether the limit counts characters or UTF-16 units, since section 6 already had to make that distinction for a password and got it wrong once.
- **Whether section 7 should refuse a grant of `cell.request_leadership` wider than `SUBTREE_EXCL_SELF`.** Section 10's "no holder of the capability, at any scope, may name themselves" is now a domain check, so the rule holds however the grant is issued. What is unsettled is whether a wider grant should be refusable at all: section 7 permits Admin to grant beyond a role's defaults, `WHOLE_CHURCH_ONLY` refuses only grants that are too *narrow*, and neither section has a mechanism for a capability whose scope value carries a prohibition. A Whole Church grant of this capability is legal today and means strictly less than it appears to — the holder may name anyone in the church except themselves — which is defensible and is not what an administrator issuing it would necessarily expect. Not blocking, because the prohibition is enforced in the domain layer. Settle it if a second capability ever takes a scope value that carries meaning; one instance does not justify a general mechanism.
- **How a requester sees the outcome of a request they submitted.** Section 19 puts "the outcome of a Cell leadership request the user submitted, of either kind" in every user's own outstanding work, and section 7 names no capability for such a route. `cell.request_leadership` is `SUBTREE_EXCL_SELF`, so it resolves against neither the caller nor the church; `cell.approve_leadership` is Admin's alone and is what guards the queue; and section 7's no-capability exemption is narrower than it looks — its examples are "reading their own claims, signing out, ending their own sessions", which is the caller's *session* rather than rows their account created. Three answers look defensible and none is derivable: widen that exemption to an endpoint returning only rows the caller created, add a twenty-eighth capability, or read it under `cell.view_subtree` against an `actor` target — which is the shape `GET /people/duplicate-candidates` already uses for a church-wide read one domain over, so this is a new reading of an existing capability rather than an unbuildable surface. Not blocking, and the Admin queue built in this slice is the half approval actually needs — the requester's view is a dashboard tile and there is no dashboard yet. Settle it with the first screens, which is also when its shape will be visible.
- **Whether Section 7's closed-Cell fallback survives for a capability that resolves as a write.** §7's base bullet gives a Cell target "the Cell's leader as of the period being viewed, falling back to its last leader where the Cell is closed", and gives that fallback a reason tied to §10 and §15: a closed Cell keeps its history and its roster visible to the leader who led it. Its closed-Cell clause, one bullet down, says the opposite about the same case — "a closed Cell has none, so every write against one resolves through nobody… Nothing else does — not a membership, not a leadership, not a configuration change." Both are §7 and they disagree. The code implements the fallback for **every** Cell target: `leaderForScope` carries no `ended_at IS NULL` filter, deliberately and with a docblock saying so, and the writes are refused a layer in instead — `POST /cells/{id}/members` answers `INVARIANT_VIOLATION` on a closed Cell rather than `SCOPE_DENIED`. No Cell-targeted **write** is admitted on a closed Cell, with one carve-out Section 7 makes deliberately, and it is stated as §7 states it rather than as the one endpoint that instantiates it today: **recording or correcting** a Cell meeting whose month's submission window is still open, which resolves through whoever led the Cell on the meeting's date. `POST /cells/{id}/meetings/{meeting_id}/submit` is the recording half; **slice 2c's correction, reschedule and `NOT_HELD` routes are the correcting half and are inside this carve-out too**, which a version of this sentence naming only the endpoint did not say. Of the rest, and checked rather than assumed: membership, category, schedule and closure each answer `INVARIANT_VIOLATION` from an explicit branch, and removing a member answers `NOT_FOUND` — that last one refused by *data* rather than by a state check, since migration 0009's trigger leaves a closed Cell no open membership to remove. So outside that carve-out the two readings differ only on **reads**, and on **one** live route — it was two until 2026-09-04, and the count has now been wrong in both directions. *The first version of this sentence said "no Cell-targeted write is admitted" flatly, which is true of `{ kind: 'cell' }` and false of `{ kind: 'cell_meeting' }` — two widths for one term, with the narrower never stated, in a bullet slice 2c's new meeting writes will be read against.* *`GET /cells/{id}/members` was the first and left on 2026-09-04, settled as decision 0204: it now carries `cell.view_subtree`, a viewing capability, so §7's base bullet governs it cleanly and this tension no longer reaches it. What that did **not** deliver is a closed Cell's roster — closure ends every membership and the route lists current members, so a closed Cell answers an empty list whoever asks.* **`GET /cells/{id}/meetings` is what remains, and no roster-capability ruling touches it.** It carries `cell.take_attendance` against a `cell` target — deliberately, on §7's own argument that requiring a management capability to reach an attendance surface is what must not happen — so it is a recording capability under decision 0186 and the closed-Cell clause would deny it, while the base bullet admits the Cell's last leader. A green case pins the admission (`cell-meetings-listing.e2e.spec.ts`, the closure-boundary case, which lists as the former leader of a closed Cell). It is also the harder half: `leaderForScope` is undated and consults no submission window, so that admission survives **after the month's window shuts**, where §7 says of the meeting exception that "once the window shuts, that too resolves through nobody". Whether a Cell-targeted *listing* under a recording capability keeps the fallback past the close is the part §7 does not decide at all. Not blocking. Decision 0186's discriminator decides which of the two *resolutions* a capability gets and deliberately does not touch the fallback; the tension predates it. Raised by `architecture-guardian` on the fix batch for that ruling, which had worded the default as though it decided this, and widened by the pass after that, which found the bullet naming one route where there are two — the second being the one its proposed dissolution does not reach.
- **Whether the archived-and-merged refusals should be database constraints.** Section 10 gained three refusals on 2026-08-29 — an archived Person, a merged Person, and somebody already in the Cell — and the first two are the same rule `assertLeaderIsAssignable` enforces for a pastoral edge. Both are application-layer checks: contrary to what Section 10 said when the question was first written, `pastoral_assignments` carries **no** constraint for archived-or-merged either, so there is no asymmetry and the question is whether *either* should become one. The Definition of Done says an invariant expressible as a constraint exists as one, and this one is expressible — a membership under an archived Person is the corruption Section 3 refuses when archiving somebody who leads a Cell, reached one relationship over. What argues the other way is that both facts live in `people`'s tables while the constraint would sit on `cells`', so it is a trigger reading across a module boundary rather than an index. Not blocking: the checks refuse today and answer `INVARIANT_VIOLATION`; what a constraint would add is enforcement under a restore, which is the argument the Senior Pastor slot and the root seat both turned on.
- **Whether a path identifier should be validated as strictly as one in a body.** `class-validator`'s `@IsUUID()` pins the version and variant nibbles and is on every DTO; `isUuid` — the repository's own predicate, used by the guard and by `UuidParamPipe` — does not. So `POST /cells/{id}/members` refuses as `person_id` a value the `DELETE` beside it accepts in the path. Every identifier in the database is a v4 and PostgreSQL's `uuid` takes both, so nothing is broken; what is unsettled is which predicate the API means, and Section 3's provision for a client-generated Person UUID is the case that would decide it.
- **Whether the nil UUID should be reserved.** Two call sites now hand `authorize` `00000000-0000-0000-0000-000000000000` — the capability guard, for a Cell it cannot place, and `CellsLeadershipRequestService`, for a handover whose Cell must not be shown to exist; the constant is shared in `common/identifiers.ts` rather than copied. It is handed over as the target of an object the caller cannot be shown to exist, so that an absent Cell refuses exactly as an out-of-scope one does. Nothing today can create a Person with that identifier — no endpoint accepts a client-supplied `id`, and every column defaults to `gen_random_uuid()` — but nothing forbids it either, and a Person holding it inside an actor's subtree would make every unplaceable Cell "covered" for that actor. The sentinel-free equivalent is to let the port's null reach `scopeCovers` the way `personBehind` already does for an absent Account. Settle it if Section 3's client-generated identifier is ever built.
- **What Section 8 permits a refusal to reveal by its existence.** The source-Cell refusal no longer names a Cell or asserts a membership, but its *shape* still carries one bit: with the actor authorized over their own Cell and any `person_id` in the church — and Section 8 publishes every Person's identifier church-wide — a 403 means that person holds a membership somewhere the actor cannot see, and a 201 means they do not. The quiet outcome is the hit and the loud one is the miss, which is the reverse of the arrangement the 2026-08-22 create-probe ruling was willing to accept, and that ruling closed the leak rather than resting on loudness. This one cannot be closed by redacting anything: the refusal is required by the authorization rule itself. The source-Cell reading it used to defer to was settled by the closure pre-flight above, which did not answer this: what a refusal may disclose by *existing* is still open, and is now the last part of that question standing.
- **Whether "Admin" in Sections 2 and 10 is a role requirement or a description of who holds the capabilities.** Section 2 settled this once, for the tree import, in the direction of "the role is required, and the capabilities alone are not enough" — and stated it in that paragraph rather than as a general rule. Direct creation is given to Admin in the same section and again in Section 10, and slice 2 reads it the same way and checks the role. If that reading is right, the two places should say it in the words Section 2 already uses for the import, because the next reader derives it from a neighbouring paragraph or not at all. If it is *not* right, then Section 7's permission to grant `cell.approve_leadership` explicitly makes request-and-approve optional for its holder over their own subtree, and Section 10 needs to say why that is acceptable. Nothing is blocked either way: the conservative reading is what is implemented.
- **Whether a Cell's first leadership row may be corrected to a leader of the other Network, and whether a closed leadership row may be written at all.** Two halves of one question, both raised by the fourth review pass. Migration 0009 refuses a Section 5 correction that closes a Cell's first leadership row and opens one naming a person of the other Network: the zero-length row is selected as the predecessor and the leader-to-leader Network rule fires. That may well be right — a Cell created under a wrong-Network leader had the wrong Network for its whole life, and Section 10 gives `CREATED_IN_ERROR` for a Cell that should not exist — but Section 10 states that rule about a *handover*, and nothing distinguishes a correction from one. The second half is narrower and has no answer at all: `cell_leadership_is_opened_open` now refuses a leadership row written already closed, because no operation Sections 10 or 11 define writes one, and that forecloses correcting a closed historical stint. Neither is reachable today. Settle both with the handover-approval endpoint, which is where Section 10 makes the refusal.
- **What the duplicate-candidate lookup does when its list exceeds `limit`.** `GET /people/duplicate-candidates` computes every candidate, returns `visible.slice(0, limit)`, and answers `next_cursor: null` — which §22's pagination rule reads as "this is the last page" over a set that was truncated, with no cursor to reach the rest. The `slice` is pre-existing; the ordering rule settled on 2026-08-28 (below) is what makes it consequential. In-scope candidates now always precede withheld ones, so the withheld tail is the **first** thing a truncation removes — the cross-branch duplicate §3 says the church-wide lookup exists to catch — and the client chooses `limit`, down to 1. Not *exactly* those: at a `limit` below the in-scope count it drops in-scope candidates too, and the point is which candidates it reaches first. Three answers are defensible and none is derivable: page the list honestly, refuse to truncate it at all, or state in §3 and §22 that the list is truncated and which candidates may be dropped. **The in-scope group's own internal order has to be settled in the same ruling**, because a page boundary over an unordered set is not pageable. `findDuplicates` issues its population query with no `ORDER BY` and `findCandidates` sorts by tier alone, so within one tier the in-scope order is PostgreSQL's physical row order. That is no disclosure — everything in that group is fully visible to the viewer — but it means that below the in-scope count, *which* in-scope candidates survive a truncation can differ between two identical requests. Raised by `architecture-guardian` on `fix/duplicate-candidate-oracle`, twice, and by the ordering rule itself: §3 now asks that any new decision the list is subjected to — it names a narrowing, a sort, a page boundary and a count — be treated as a disclosure until it is shown to be a function of what the viewer may already know. This is the one page boundary that exists, and nothing has shown it. Not blocking while the default limit of 50 exceeds any candidate list this church produces.
- **Whether a pastoral path renders an absorbed Person or the survivor.** Every other `persons` read in the application filters `merged_into_id`; the path's name lookup deliberately does not, because on a lookup a filtered row is simply not found while on a path it is a *hole*, and a path with a hole reads as a shorter chain rather than as an error. Section 3 also says a merge never rewrites pastoral records to point at a different Person, so an absorbed ancestor genuinely stays on the chain and the real question is whether to show them or the survivor who now carries the identity. That is Person Merge's to answer for every surface at once rather than this endpoint's to decide, and merge is Stage 3, so nothing today can reach it. Settle it with the merge.
- **Whether a Person holding an open root row may be absorbed by a Person Merge.** §3 refuses a merge where the absorbed Person leads a Cell and says nothing about a root; §5 leaves succession undefined and forbids reassigning a root. So merging a duplicate root holder into the real person appears permitted — and §3 says a merge "never rewrites historical attendance, pastoral, or audit records to point to a different Person", so the seat row keeps naming the absorbed record. The resolved identity then has two open assignments, which §5 invariant 3 forbids and which no constraint can refuse, because the rows carry different `person_id`s. Raised by the fifth review of the tree import, which is the only thing in the system that creates root rows: the dry-run warning's whole force is that no remedy exists for a mis-seated root, and merge is the one remedy §3 offers for a record created in error. The warning is correct under §3 as written. Settle this before anyone needs it.
- **Whether a decisions file should bind the candidate set it was adjudicated against.** The fingerprint covers the input file and says nothing about the database, and section 2's decisions file has no candidate column — so a `CREATE` acknowledges a candidate set nothing pins. A Tier 1 candidate arriving between the dry run and the commit is caught where it gives a row its *first* one, because the row is then blank or absent, and is not caught where the row already carries a decision: it is created past an acknowledgement made about somebody else. Closing it means a per-row digest of the candidate identifiers, carried in the file and compared at commit — structure section 2 does not describe. Narrow in practice while the import is thirty rows against a near-empty database, and it is the shape that would matter if this were ever pointed at a larger file. Decide it before any second use of the import.
- **Which leaders a Network's drill-down offers, and therefore whether Network → Leader is required to add up.** **A Stop Condition.** §17 fixes the chain as Whole Church → Network → Leader and never says which leaders sit at that level. §20 accepts one gap there, naming its residual -- somebody who held no assignment at any instant -- and §9 makes that class unreachable in a DCC population, because a Person with no open assignment row cannot have a record. The class that *is* reachable is different and wider: anybody whose pastoral chain terminates outside the tree, which `reporting-leader-scope.spec.ts` now builds and which produces the same gap. So §20 names one class where there are two. If the Network level is the Network's **root**, the gap is real and §20 should name both; if it is **every chain-terminating leader in the Network**, it adds up and neither gap exists. Nothing decides it, and the first Network drill-down screen implements one of the two. Raised by `architecture-guardian` on decision 0219.
- **Whether a Person outside the pastoral structure may acquire disciples.** **A Stop Condition**, load-bearing for evidence rather than for behaviour. §5 permits an administrator to hold zero pastoral assignments and §6 creates exactly one such Person; nothing says whether a pastoral edge may *point at* them, and `assertLeaderIsAssignable` admits it -- it checks merged, archived and Network, and never asks whether the leader holds an assignment. Decision 0219's evidence now rests on that state, as does `reporting-leader-scope.spec.ts`'s additivity gap, because it is the only reachable way to be in a Network and in no walk from its root. So a rule closing it would invalidate two fixtures and change no figure. Not reachable by accident -- an administrator is created deliberately -- and not blocking. Settle it before anything relies on the state being *permitted* rather than merely unrefused. Raised by `architecture-guardian` on decision 0219.
- **Whether two `network_assignments` rows for one Person may be in force at one instant, and what a Network-scoped figure does if they are.** **A Stop Condition**, and the exact counterpart of the `pastoral_assignments` overlap bullet below, with the same remedy — `EXCLUDE USING gist (person_id WITH =, tstzrange(started_at, ended_at) WITH &&)`. §4 defines a Network change as a close-and-open pair sharing one instant and says nothing about historical overlap; `network_assignments_one_open` is partial over **open** rows only. §20 now publishes an exact identity that this decides: Men's + Women's equals the Whole Church total. `peopleInNetworkAsOf` fans out over every matching row, where `network_as_of` takes `ORDER BY started_at DESC LIMIT 1` — so an overlap naming two Networks counts one person in **both**, and the sum exceeds Whole Church silently, where §20's precedent for a non-functional placement graph is to **refuse** the figure. Not reachable through any write path today: the only Network writer closes and opens at one instant. Today the identity also leans on two things §20 does not state — a DCC record requires an open assignment (§9), and a first Network assignment cannot precede encoding — neither of them a constraint. Raised by `architecture-guardian` on decision 0219, which is what made the identity exact enough to be broken.
- **Whether closing a person's only open `network_assignments` row, without opening a replacement, is a legal write.** Escalated by `architecture-guardian` on 2026-08-25 and general rather than root-specific. §4 defines a Network change as an atomic close-and-open pair sharing one instant; §5 forbids `DELETE` on the table; nothing addresses a close alone. No constraint refuses it — `network_assignments_one_open` is partial and permits zero, and the same-Network trigger compares at the closed row's own start, so it passes. The consequence is that `network_as_of` becomes null from that instant and every edge beneath the person is silently unresolvable, which is the outcome the no-delete trigger exists to prevent, reached one column over. The root seat made it visible rather than causing it, and the root case is now refused specifically; the general case is not. The same silence covers a close at T1 reopened at T2, which leaves a gap with no Network at all. **It now reaches one level further than this bullet said**: since decision 0219 a Network's reporting population is its membership, so such a person leaves **both** Network totals while the Whole Church total keeps them, and §20's Men's + Women's identity fails downward — the mirror of the overlap bullet above, which fails it upward. Recorded on 2026-09-07, when the identity became exact enough to break.
- **Who may close a Network root's row, and under what capability.** §5 gives each Network exactly one root and says changing who holds one is "a deliberate Network-level decision, not a pastoral reassignment" — and names no capability, no endpoint and no workflow for it. The seat added on 2026-08-25 is partial over open rows, so a successor becomes possible the moment the previous root's row is closed; both write paths that could close it refuse a root outright, so nothing can. §5 now says plainly that a succession is not an operation this system offers, rather than implying one from the seat being freeable. Not blocking: the import creates two roots and neither changes.
- **Whether §2's closed exemption list gets a gate.** §2 was narrowed on 2026-08-26 to permit exactly one cross-module read and declares the list closed — with nothing able to fail on it. This repository gates the pure-client boundary, the refused UI packages, the palette token names and the module graph, each with a check that goes red; a cross-module table read is greppable in one line and has none. Where such a check would live — an ESLint rule, a test over the source, a lint script beside `check-ui-dependencies.mjs` — is the part that needs deciding.
- **Whether the three cases of §5 invariant 3 should be told apart in the data.** §5 permits zero open pastoral assignments for a Person not yet assigned, an archived Person, and an administrator outside the pastoral structure — and nothing records which. The absence of a row is the same absence in all three; the difference is in why, and the schema holds no `why`. So the first attention list that surfaces unassigned Persons shows an administrator among people genuinely waiting for a leader. §5 names the remedy as that list excluding accounts holding `ADMIN`, which needs no new structure and needs the list to exist. Decide it with the first screens. *(§5 claimed this was “recorded as open” from the moment the third case was written on 2026-08-25, and it was not — the fifth false “written to / recorded as” claim on this project, and the first found by a reviewer grepping the open list for it.)*
- **How a person's title is displayed, and whether it is stored.** §3 keeps first, middle and last names and has no title field, and since 2026-08-25 says plainly that a name field is not where a title goes — a title inside one is compared as though it were part of the name, so `Bishop Oriel` never matches `Oriel` and a duplicate goes unnoticed. What is unsettled is where it *does* go. A column is the obvious answer and is not effective-dated, so it cannot say what somebody was called in a past period — the mistake the 2026-08-20 structures ruling names. Two of them are derivable from `SENIOR_PASTOR_PERSON_IDS` already. It is a display question; decide it with the first screens.
- **Whether a leader sees a "details to collect" list.** Birthday became optional on 2026-08-24, and an optional field with nothing surfacing it is one that never gets collected. §15's attention-list idiom fits — filtered, never ranked, never colour-graded, shown to the leader who can act — but there is no dashboard to put it on. *This said "until Stage 2's screens exist", which stopped being the condition when they did: Stage 2 landed People and authentication and no dashboard, so the deliverable this waits on is the screens block, scheduled between Stage 5 and the pilot by the ruling of 2026-09-06.* Decide it there.
- **Whether "asked, not given" is a state on the Person.** It follows the item above rather than standing alone. Without it, somebody who declined to give their birthday stays on a collect-list forever, which presses on exactly the privacy the optional ruling protects. With it, the next leader learns she was asked rather than rediscovering it by asking again — but it is a new field on `persons`, so it is a ruling and not a detail.
- **Whether a recorded birthday may ever be removed.** §3 defines adding one and, since 2026-08-24, refuses an explicit null on the edit path so that a nullable column does not become an erase capability nobody decided on. The privacy argument that made the field optional cuts toward permitting removal — somebody may withdraw what they earlier gave. Reproducibility cuts the other way: a Tier 1 acknowledgement recorded against a birthday, and every age derived from it, stop being explicable once it is gone. Left refused until decided.
- **What clock skew account-wide revocation may tolerate, once there is a second API instance.** The instance count itself left this list on 2026-08-31, settled at one and written into §24, which is what makes the revocation comparison exact today: §6 requires a token's issued-at and the account's revocation marker to be stamped by an API process, and on one instance those are one clock. What is still open is the bound a second instance would need, and it is deliberately not guessed in advance — a tolerance chosen with no second host to be skewed against is a number nothing can fail, and any tolerance is a loosening, admitting near-boundary tokens an exact comparison refuses. §24 now states what the change introducing a second instance owes: a stated maximum, NTP holding it, the comparison made tolerant in the fail-safe direction, and every other cross-instance timestamp comparison found and given the same treatment. The row lock added for the uncommitted-revocation window orders the two events in the database and depends on no clock, so this reaches the comparison and not the ordering. Not blocking: nothing can reach it while one instance runs.
- **Whether the isolation level should be pinned the way `DateStyle` now is.** Section 24 records `READ COMMITTED` as a dependency and checks it in a **test** (`invariants.spec.ts`) against the test database — no deployment runs that check, so a deployment that sets `default_transaction_isolation` still silently removes an authorization guarantee, which is what Section 24 says must not happen. The `DateStyle` ruling of 2026-08-30 assumed the two settings differed in kind, on the belief that a client cannot set another session's isolation default; that is false, and `architecture-guardian` refuted it in one connection — `default_transaction_isolation` arrives in the startup packet by exactly the mechanism the pool now uses for `DateStyle`. So the option is available and was never considered. Three answers look defensible: pin it on the pool and assert it at startup, as `DateStyle` now is; assert it at startup without pinning, so a deployment that has chosen a different level is told rather than overridden; or leave it as a test-only check and accept that the guarantee rests on nobody changing the server. Pinning is not obviously right — an operator who sets `SERIALIZABLE` deliberately would find the application quietly overriding them, which is a different thing from a `DateStyle` nobody sets on purpose. Not blocking; it belongs beside the least-privilege role and the liveness probe, since all three are settings a deployment owns.
- **The application's database role.** §24 requires least-privilege credentials and none exist: the API connects as the owner of every table, so it holds `TRUNCATE`, which bypasses the no-delete triggers entirely, and `DROP`. The no-delete rule leans on this role to make its `TRUNCATE` exemption safe. Creating it is deployment work with no ruling attached, but until it happens §5's exemption is unprotected.
- **Whether a revocation may be undone in place.** Nothing addresses setting `revoked_at` back to `NULL`, and the schema permits it on `account_roles` and `capability_grants`. It erases a revocation exactly as a `DELETE` would, one column over — and the Senior Pastor cap depends on `revoked_at` being monotone for the count to mean anything over time.
- **The native client framework.** `SKILL.md` §2 settles the web stack and says nothing about Android and iOS. Deferred since the specification was written; indexed here because two rules now point at it as open.
- **What the native clients owe on accessibility.** `SKILL.md` §23 binds the web application to WCAG 2.2 AA and says the equivalent obligation for a native client is the platform accessibility API rather than WCAG. Which platform guarantees, and what would fail a build, is a ruling to make when the client is.

- **Whether the liveness probe should share the application connection pool.** §24 now records that it does, and that pool exhaustion therefore presents to the platform as a dead process — so the response is a restart that discards the transactions still making progress. A separate connection, or a probe that does not reach the database, are both defensible and mean different things by "healthy". Deployment work with a ruling attached, alongside the database-role item above.
- **Whether `cell_leadership_requests.cell_id` should be frozen while a request is `PENDING`.** Migration 0009's finality trigger freezes what a request asks *about* — its kind, the person it names, who submitted it and when — and deliberately leaves the category, day and time writable so a mistyped time is corrected rather than declined and resubmitted. It says nothing about `cell_id`, which for a `HANDOVER` names the Cell itself. Nothing writes it on a `PENDING` row today, so this is not a live hole; what makes it worth settling is that approval chooses its lock list from that value before the transaction opens, and its under-lock re-check defends the value rather than relying on the trigger. If a revision path is ever added — correcting a handover that named the wrong Cell — the question becomes load-bearing in a place nobody would think to look.
- **How a meeting status recorded in error is corrected.** Decision 0195 settles the four transitions §13 names and refuses the rest, which leaves two real cases with no route: a leader who files `NOT_HELD` and then finds the meeting did happen, and one who files `HELD` for a meeting that did not. §13 defines no such operation — it defines *moves*, each writing a `cell_meeting_changes` row that says what moved and when — and a correction of this kind is a claim that the first record was wrong, which is a different thing and would need its own audit shape. Inventing one alongside the transitions §13 does define would put an unaudited "the record was wrong" beside them. Not blocking and not urgent: both are rare, and the wrong answer is a route that quietly rewrites history. Settle it if a leader actually hits it, or with the first screens, whichever comes first.
- **Whether a reschedule may move a meeting to a date whose own month has shut.** §13 fixes `reporting_month` at creation and a reschedule never moves it, so the *record* stays in the month it reports in — but `actual_date` may name a day in a month whose window has closed, and decision 0182's amendment governs *when* a record may be written rather than *which dates it may name*. *This bullet said it was "refused today by the window check already guarding the route". It was not refused at all — that check reads the meeting's own `reporting_month`, which a reschedule never changes, so `actual_date` reached no window function anywhere and a meeting could be moved into a month shut for weeks. Reproduced by `architecture-guardian` at 201, and the same false claim stood in decision 0195.* A refusal is now written, because a record naming a day inside a closed month is what §13 shuts the window to prevent. What stays open is whether that is the **right** bound rather than whether one exists — §13 states exactly one bound on an actual date (after the Cell's closure) and says nothing about how far back or forward a move may reach. §13's own example, 31 January moved to 2 February, is a move across a month boundary and says nothing about either month's window.
- **What bounds a reschedule's `actual_date`, and three of the four bounds are the code's own.** §13 states exactly one: a meeting dated after the Cell's closure is refused, "the Cell did not exist to meet". The route applies **four**, and the other three were each chosen while fixing a defect rather than derived from the specification — the meeting may not move onto the date it already stands on; the month it moves into must be open unless the submission is an Admin amendment; and the Cell must have had at least one member on the target date. *This bullet said "two refusals" through two commits that added a third and a fourth, which is the "recorded as N" error this log exists to catch, committed against the sentence whose only job is the count.* **The empty-roster bound is the one to look at hardest**: it prevents real corruption — an empty roster makes every check below it vacuous, so the meeting ends up `RESCHEDULED` with nobody recorded and §12 counts it in N — but it contradicts the first-submission path, which records `HELD` for that same Cell on that same empty roster without complaint. Either the move is too strict or the first submission is too lax, and nothing decides which. Nothing says either whether an actual date may precede the scheduled one (a meeting held *early*) or fall arbitrarily far ahead, and the route applies neither. Not blocking. Settle it when a real reschedule needs a bound the code does not have — and note that §13's own example, 31 January moved to 2 February, is a **forward** move across a month boundary, so a naive "must not precede the scheduled date" would be the easy wrong answer. Raised by `architecture-guardian` on the transitions slice, twice.
- **What `facilitated_by` may name, beyond a Person who exists.** The 500 is closed: decision 0199 translates `cell_meetings_facilitated_by_fkey` into an `INVARIANT_VIOLATION`, so a well-formed body naming an absent Person is refused rather than reaching the constraint. What stays open is the narrowing, which is what made it a ruling rather than a fix. §13 says only "who conducted this meeting", and three readings refuse different bodies: any Person, a member of that Cell on the meeting's date, or someone in the responsible leader's subtree. Existence is the part all three share and is therefore the part enforced without settling the rest. *The constraint enumeration recorded here earlier — that this was the only foreign key on the three attendance tables taking an unvalidated client value — held, and is why the translation could be narrowed to one constraint by name: the other three take server-derived values, so one of those failing is a defect and still fails loudly.* Settle the narrowing with the first screen that offers a facilitator picker, which is also the first thing that needs to know which people to list.
- **How a refusal inside a nested object names the field that broke.** §22 asks a refusal to name the field "which is what a client needs in order to fix it", and for the three of the twenty-two storable-text fields that live inside a nested DTO it does not: `records[0].correction_reason` answers `{ "field": "records", "problems": [] }`, and `amendment.reason` answers `{ "field": "amendment", "problems": [] }` — the container, with no message. The shape is the error filter's and is the same for every nested refusal on every route, so this is not storability's to fix; §22 now states the limitation rather than overstating the guarantee. What is open is whether the filter should flatten a nested path (`records[0].correction_reason`) and carry the inner messages. Settle it with the first screens, which is the first thing that will need to show a client which line of a roster it got wrong.
- **What a `correction_reason` means on a first submission and on a transition to `NOT_HELD`.** §14 asks for a reason "as appropriate" when an already-recorded meeting is changed, and the Cell submit route now honours the field on two of its four paths and silently drops it on the other two. A **first submission** carrying one answers 201 and stores it nowhere — there is nothing being corrected, and the DTO asserted for weeks that it "is refused" there, which it never was. A **transition to `NOT_HELD`** also drops it, because that branch writes `not_held_note` into the one column `cell_meeting_changes` offers. Neither is new — both predate the transitions slice — but the reschedule fix has made the asymmetry sharper than the uniform drop it replaced, which is why it is recorded rather than left to be rediscovered. Three answers are defensible and none is derivable: refuse it on a first submission, which is what the DTO already claimed; store it on the `NOT_HELD` transition, which then has to say what happens when a leader sends both a `correction_reason` and a `not_held_note` for one column; or state in §13 that the field is a correction-path field and is ignored elsewhere. Not blocking — nothing is corrupted and no refusal is wrong — but a field accepted and discarded is the shape §22 says can never be given meaning later, so the third answer costs the least and is still a ruling. **`not_held_note` has the identical shape and is named here for the same reason**: a second `NOT_HELD` submission carrying a changed note answers 201 with `corrected: 0` and leaves the stored note as it was, and a `HELD` first submission carrying one stores it nowhere. The first version of this bullet named only `correction_reason` — one rule, two fields, recorded for one, which is the pattern the ruling it was written beside exists to condemn, applied to this list itself. Raised by `architecture-guardian` on the tenth pass and widened by the eleventh.
- **Whether two `pastoral_assignments` rows for one Person may overlap in `[started_at, ended_at)`.** §5 constrains the **open** row only — `pastoral_assignments_one_active` is partial on `WHERE ended_at IS NULL` — and nothing bounds historical overlap. Half-open closes the shared-instant case of a reassignment and does not close this one: two rows in force at one instant, naming two leaders both inside a walked subtree, put the person in the result **twice** by two paths, which the `CYCLE` clause correctly does not flag because it is not a cycle. That is the duplication §5 invariant 3 says must never reach a §20 unique-people total, arriving one column over from the constraint that forbids it. **Not reachable today**: the only writers are `openAssignmentWithin`, which writes a Person's first row, and `reassignWithin`, gated by `backdateFloorFor(..., 'as-subordinate')` whose own comment says it exists so that "an effective date inside an already-closed period would leave two rows valid at one instant". So the property is held by an application-layer floor, and the Definition of Done asks that an invariant expressible as a constraint exist as one — this one is expressible exactly, as `EXCLUDE USING gist (person_id WITH =, tstzrange(started_at, ended_at) WITH &&)`. What is undecided is whether non-overlap is a rule of §5 or an accident of the floor. **A Stop Condition raised by `architecture-guardian` on `subtreeAsOf`**, which was the first query whose *result* is wrong rather than merely arbitrary if an overlap ever exists; every earlier as-of read answers with one row and survives one. **`reportingSubtree` is the second, and it depends on non-overlap in a second way this bullet did not name**: its `in_force` set carries no `DISTINCT ON`, so an overlap duplicates a person into two subtrees — and its fallback orders by `started_at DESC`, which under an overlap picks the later-*starting* row even where an earlier-starting one ran longer, attributing the person to the wrong leader. That second failure leaves **both §20 identities intact**, so the reconciliation test the Definition of Done requires cannot see it. **There is a third, found by `architecture-guardian` on the fix batch and now pinned**: `reportingSubtree`'s whole-graph cycle check is `grounded`, which asks whether a person has *any* terminating chain — so a cycle member holding a second, grounded edge grounds the whole cycle and `has_cycle` is false. A leader whose walk does not enter that cycle is then answered cleanly over a graph that holds one, which is the silent truncation §20 refuses. The walk's own `CYCLE` flag does not cover it either: it marks a person repeated on that row's own *path*, so it catches a cycle the walk enters and never catches a person reached twice by two paths. The `EXCLUDE USING gist` constraint the bullet proposes closes all three; the enumeration said two, which is the count-in-the-sentence-whose-job-is-the-count error this log keeps recording. `reporting-subtree.spec.ts` pins the two miss cases as the behaviour the method currently has, so settling this cannot forget them — both go red the moment `in_force` is made functional. **What the *report* does when the premise fails was settled on 2026-09-05 as decision 0212** — where any person holds more than one edge in the placement graph, every figure computed from it refuses, detected over the whole edge set as a cycle already is — so the figures stop being silently wrong while this stays open. *That ruling's first draft scoped the refusal to the walk that saw a duplicate, which `architecture-guardian` falsified by reproducing the sibling case: each sibling's walk holds the person once and neither refuses, while every total above holds them twice.* That ruling deliberately settles nothing here: whether two rows may overlap at all is still this question, its remedy is still the constraint above, and 0212 stops being reachable the day that constraint exists.
- **What a report owes an overlap that opens and closes inside the period.** Decision 0212 refuses every figure where a person holds more than one edge in the placement graph, and `architecture-guardian` established by replaying the query that this reaches an overlap **in force at the period's end** and no other. Two rows that both open and close within the period are collapsed by the fallback's `DISTINCT ON` to a single edge, so the graph stays functional, nothing refuses, and the person is placed by the later-*starting* row — which may not be the leader they were actually under for most of the period, and which leaves **both §20 identities intact**, so the reconciliation test the Definition of Done requires cannot see it. It is the same mis-attribution the overlap bullet above already names, in the one subset 0212's refusal does not cover, and it is recorded separately because that bullet reads as though the refusal now covers the class. Not reachable today, for the same reason: no write path produces overlapping rows, and the `EXCLUDE USING gist` constraint proposed above closes this with the rest. Raised by `architecture-guardian` on the fix batch for 0212.
- **Whether §20's placement graph may serve as a visibility grant for a per-person view.** **Narrowed on 2026-09-06 by decision 0214, which took the aggregate half**: a report's scope selector resolves through the pastoral tree in force at the period's end and never through the placement graph, so the wider graph computes and the narrower one authorizes. What stays open is the third answer that bullet named and 0214 half-took — whether a **per-person drill-down** may be authorized on the wider graph. It cannot be settled by the aggregate ruling, because the aggregate discloses no identity and a drill-down does: that is the surface where the difference between the two graphs stops being an arithmetic one. The case is unchanged. `X` holds an open assignment under `D`; `D` left in 2020 and `D`'s last row, from 2019, named `U`. On the placement graph `X` is inside `U`'s subtree, and decision 0209 treats that very condition as a gap to be surfaced on an attention list rather than as a relationship. Not blocking: no per-person reporting view exists. Settle it with the first one.
- **Whether a report scope selector may name a Person who does not exist.** **A Stop Condition**, raised as an escalation and labelled as one rather than filed as an ordinary item. §7 makes the selector the target and says nothing about whether it must resolve to anybody. `reportingSubtree` seeds its walk with the identifier unconditionally, so a Whole Church holder asking for any UUID receives a well-formed report of zeroes rather than a refusal — including `NIL_UUID`, which `common/identifiers.ts` describes as "a target that resolves to nobody" and hands to `authorize` for "an object the caller cannot be shown to exist". Nothing is disclosed: the payload is zeros either way, and a Leader is refused by scope before the question arises. What is undecided is whether a report for a Person who does not exist should be a `404`, a refusal, or the zero it is — and the sentinel appearing as an accepted selector is the part worth looking at, since every other use of it means the opposite. Not blocking. Settle it with the first screen that offers a leader picker, which is the first thing that will need to know which selectors are offerable. Raised by `architecture-guardian` on the first reporting route. **It reaches a second target kind since 2026-09-08**: a `CELL` selector naming a Cell that does not exist is refused to every narrower grant, because it resolves through nobody, and answers a Whole Church holder zeroes for the same reason a `LEADER` selector does — `scopeCovers` returns true at Whole Church before the target is read. Decision 0220 and §7 both asserted the refusal unqualified for one commit, and the case pinning it used a subtree grant, which is the half where the claim held.
- **Whether a leader inside an actor's own total, but outside their reach, should be requestable.** Decision 0214 authorizes a report's scope selector on the tree in force at the period's end while §20 computes the population on the wider placement graph, so the two differ by exactly the population decision 0206 exists to add. The consequence is stated in 0214 and not settled by it: a leader `L` archived on 15 October sits inside `A`'s October total, because `reportingSubtree`'s within-period tier selects `L`'s row, and is refused to `A` as a selector, because the dated walk runs at the period's end where `L` holds none. `A` sees a figure and cannot ask for the breakdown that explains it — which is, on the graph axis, the defect decision 0207 removed on the date axis, in that ruling's own words. It reaches every leader archived or unassigned mid-period, which is the population §3 forbids filtering out of a period-based report. 0214 accepts it because the alternative — measuring reach on the placement graph — lets a leader read the figures of somebody whose only connection is a chain through a person who left years ago. A third shape nobody has costed is to admit a selector the actor's own total already contains. Not blocking: no reporting route exists. Raised by `architecture-guardian` on decision 0214.
- **What a DCC monthly report does when N is zero, and whether a month with no calendar rows is reportable at all.** §12 refuses a `Completed (0/0)` bucket for a **Cell** — a bucket every person satisfies is not a bucket — and §9 states no N = 0 rule for DCC. The reporting slice derived the DCC answer by analogy and then pinned it with a test, which is a rule invented at a keyboard. Worse, N = 0 conflates two states §9 works to separate: a **removed** Sunday "always means a row that records a decision", while a *missing* row — before the calendar start, past its horizon, or left by a lapsed generation — "is never a decision", and §9 says a report covering a month with a gap is **wrong until the gap is filled**. The current behaviour answers a clean zero for the gap case. Not blocking: the calendar runs twelve months ahead and no month in use has a gap. Settle it by having §9 say what a DCC report does with N = 0 and whether a gap month is reportable. Raised by `architecture-guardian` on the first reporting slice.
- **Where a merged Person's identity is resolved for reporting.** §3 requires a merged pair to count as one person "when reports are generated", for every period including past ones, and §2 places identity in `people`. `DccFiguresService` groups by raw `person_id`, so both the unique-people total and every lifetime classification would be wrong for a merged pair. **Not reachable today** — nothing writes `merged_into_id`, because Person Merge is unbuilt — and named because the query's docblock enumerates what the population excludes and a reader would take that list as complete. Settle it with the merge, which is the change that makes it reachable, and note it binds every scope and both domains rather than this one query.
- **What §20 means by "the figure refuses" when the placement graph holds a cycle, and how such a cycle is repaired.** The refusal implemented is church-wide for the period: `has_cycle` ranges over the whole edge set, so a cycle confined to the Women's Network refuses the Men's root its own figure. §20 does not fix the blast radius. Three things make that consequential rather than merely conservative: §20 itself says the cycle arises from **two writes that were individually legal**, so the error text blames nobody in particular; decision 0209 refused "refuse the whole report" for the *dangling-parent* case precisely because it punishes a leader for another branch's state, and does not say why the argument stops here; and **there may be no repair** — §5 forbids deleting an assignment row, breaking the cycle needs a write *into* the closed period, and the backdate floor refuses an effective date at or below the person's open row's `started_at`, so once either party has a later open assignment the period is permanently unreportable. Not blocking: no write path can create such a cycle today. Settle the blast radius and the remedy together. Raised by `architecture-guardian` on decision 0209's implementation. *Decision 0212 puts the neighbouring **duplicate** refusal under this same question rather than answering it separately: it refuses over the whole edge set, exactly as the cycle rule does, so the blast radius to settle is now one radius covering both. A draft of that ruling scoped the duplicate refusal narrowly and offered the discriminator that a duplicate's damage does not leave the total containing it while a cycle's does; `architecture-guardian` reproduced the refutation — one person under two sibling leaders leaves each sibling's walk clean and every total above wrong — and the clause survived one commit after the ruling it described was rewritten. Nothing here supplies a discriminator; whoever settles this should not reason from that one.*
- **Where a person who held an assignment before the period, none within it, and leads nobody is placed.** §20's placement rule reaches only a *leader* the chain arrives at, and the residual sentence — as amended by decision 0209 — now excludes anybody who held an assignment before the period, so this class is placed by no rule at all. The code excludes them from every leader's subtree, and *that* is what is pinned. The exclusion is the defensible reading; what is wrong is the amendment, which widened the exclusion past the leader class it needed to rescue. *This sentence said "a Whole Church total is where they land under §20, and nothing in the repository computes one yet". Both halves are now false: the Whole Church total has been computed since `GET /api/v1/reports/dcc/monthly` merged at `706bc6f`, and since decision 0219 such a person lands in their **Network's** total too — which is the amendment that ruling made to §20's residual sentence, reproduced here unchanged because this bullet was not swept with it.* That reintroduces, inside the specification, the very shape 0209 was written to close: a class nothing detects falling outside the additivity claim. Narrow §20's residual sentence to name the leader class. Not blocking; the behaviour is already right.
- **Whether §20's archived-leader attention list keys on lifecycle or on the condition it exists for.** §20 requires a list of people whose pastoral leader is **archived**, on §15's terms. The extended chain fires on a leader holding *no assignment within the period*, and §5 invariant 3 gives three legitimate causes of that — archived, encoded but not yet assigned, and an administrator outside the pastoral structure. As specified the list surfaces one of the three and misses the others. It also has nothing that can fail: §15 is unamended, §19's dashboard is unamended, and there is no code and no test, which is the shape §23 is gated against. It is a screen, so it belongs with the screens block — which the ruling of 2026-09-06 scheduled, between Stage 5 and the pilot, partly because this bullet said it was owed to "a stage nobody has scheduled it in". Recorded here so it is not lost between a specification requirement and the stage that now owes it.
- **Whether `audit_log`'s append-only guarantee tolerates `TRUNCATE`.** §5 records the exemption for history tables and leans it on a least-privilege role that does not exist, which is already open above. §21 says nothing at all, and the test suite truncates `audit_log` before every test. Same answer as the `TRUNCATE` question above, most likely, but it is not written down for the one table whose whole purpose is that nothing removes a row.
- **What a lost first submission answers when the two writers disagree about the meeting's *status*.** §22 states its two lost-race outcomes over "the line" — `VERSION_CONFLICT` where it still disagrees, `RESOURCE_BUSY` where it now agrees — and a `HELD`/`NOT_HELD` disagreement has no line to compare, because a `NOT_HELD` body carries no attendance at all. Neither outcome fits: the `VERSION_CONFLICT` body has no status field, so a client cannot see that the meeting was filed as not held, and `RESOURCE_BUSY` is false of it, because §13 makes a status change a separate operation and the retry that code prescribes is refused permanently. **Implemented conservatively rather than settled**: the loser is answered what the identical body would be answered sequentially against the committed state, which is the `INVARIANT_VIOLATION` §13 gives a status change, and `cell-meeting-submit.e2e.spec.ts` pins the raced and sequential answers as the same code. That removes the timing divergence and invents no rule; what is open is whether that is the *right* answer, or whether §22's conflict body should carry a status so the two writers can choose between them. Raised by `architecture-guardian` on the first-submission race, which reproduced the `RESOURCE_BUSY` and its permanently-refused retry.
- **Whether §7's amendment gate is *owed* by a submission that wrote nothing at all.** The cross-domain half is settled: decision 0201 gates the disagreeing branch of a lost race in both domains, so an actor holding only the recording capability is no longer handed the stored figure and the recording account's name by losing a race, where sending the identical body sequentially answers `403`. That was a disclosure rather than an inconsistency, and it is closed. What stays open is the question underneath it, which §7 still does not state: a first submission that loses a race and disagrees **amended nothing** — it wrote no row — so whether the amendment capability is owed there at all is a reading rather than a rule. It is implemented in the conservative direction in both domains, because the cost of gating a disclosure that turns out not to need gating is a capability somebody can be granted, while the cost of the reverse is a figure published to an actor who cannot read it any other way. The agreeing branch is not open: §7 settles it, a write that writes nothing owes no amendment capability (decision 0191), and gating it would refuse `403` what the identical body answers `201` — with §22 storing the 4xx against the idempotency key, so a conforming retry would replay it for ever.
- **What a `NETWORK`-scoped Cell figure narrows.** **A Stop Condition**, and the reason `GET /api/v1/reports/cells/monthly` offers `CELL | LEADER | WHOLE_CHURCH` and refuses `NETWORK`. §20 says a Network narrows "which people the **person key** runs over" and adds "no fourth attribution key: … a Cell-scoped one still [is attributed] by the meeting's responsible leader" — so for a domain that does not use the person key, the narrowing has no stated target. Two readings, and they are not obviously different: narrow by the **responsible leader's** Network at the instant, or by the **attendees'**. §10 requires a Cell's members and its leader to share one Network and `cell_leaderships_stay_in_network` holds it for leaders, so the two coincide **except across a Network change** — which is what makes this narrow rather than unreachable, and what will make it missed. It also decides whether §20's Men's + Women's = Whole Church identity is claimed for this domain at all: under the leader reading it would rest on a **third** unenforced property, a Cell's leader holding exactly one Network row at the instant, on top of the two the `network_assignments` overlap bullets already name. Not blocking: the scope is refused at the guard, which declares no `networkFrom` for that route. Raised by `architecture-guardian` on decision 0220.
- **Where a responsible leader who held an assignment before the period, none within it, and whom no edge of the placement graph names as leader, is placed, and what a Cell figure owes their attendees.** **A Stop Condition**, and the gap decision 0221's second residual was written to close and does not. That residual covers a leader "in that state" — the state the sentence above it defines as no open assignment at **any** instant of the period *and none before it either*. `reportingSubtree` drops a second, different class from every subtree above: a leader who held an assignment **before** the period, none **within** it, and **whom no edge of the placement graph names as leader**. *The class was recorded as "leads nobody" for one commit, on the `departed` tier's base case alone — `EXISTS (… primary_edges p WHERE p.leader_id = le.person_id)`. That tier is **recursive**: it unions a second arm joining `departed d ON d.leader_id = le.person_id`, so it admits a leader who leads somebody already placed, and then a leader who leads one of those. A leader whose only disciples are themselves unplaced departed leaders therefore leads somebody and is dropped just the same, and the narrower wording would have had this settled for a class smaller than the gap. Reproduced by `architecture-guardian` against `dfc_ci` with a three-deep chain closed before the period.* **The person-key bullet above carries the same "leads nobody" wording, and so does the `reportingSubtree` docblock in `api/src/hierarchy/hierarchy.service.ts`** and is pre-existing on `main`; it is flagged here rather than rewritten, because whoever settles either should check whether the same recursion widens both. Reproduced by `architecture-guardian` against `dfc_ci`: with such a leader's meetings attended by somebody sitting inside a root's own pastoral subtree, that root's Cell figure is **0** while Whole Church is **1**. That is the additivity failure decision 0206 exists to prevent, arriving through the key 0221 has just given a placement rule. **`CLAUDE.md` already records this class for the *person* key** — the bullet above beginning "Where a person who held an assignment before the period" — and closes it "Not blocking; the behaviour is already right." **That ground does not carry to this key**, which is why this is a separate bullet rather than a widening of that one: dropping an unplaceable person from a figure that counts *them* is defensible, while dropping a responsible leader removes **other people's** recorded attendance from a leader who genuinely disciples them. Not reachable by accident. Escalated rather than settled — §20 states nothing about where this class lands for the responsible-leader key, and 0221's residual is not stretched to cover a case whose consequence differs. *This ground was true of §20 and **false of 0221** for one commit: §20 says "in that state", carrying its own definition's "and none before it either", while 0221 said "no assignment at any instant of the period" and so covered this class by wording alone — a question recorded as open beside a shipped ruling that answered it, which is the pattern this log counts. The wider wording was also **false** of the neighbouring sub-class, a leader in the same position who does lead somebody, whom the `departed` tier places. Both homes now state the narrow class, and §20 now carries a marker that neither residual is exhaustive.*
- **Which month's classification an in-progress year carries.** **A Stop Condition.** §12 makes classification a lifetime count truncated at the end of the reporting **month**, and decision 0222 makes a yearly report return the months that have begun and omit the rest. For a year in progress those two leave the year's own classification undefined: the obvious answer, December's value, names a month that report does not cover. *Both decision 0222 and §20 asserted "December's value" for one commit; it was written by the fix batch of 2026-09-08 rather than by the ruling, it contradicts the rule the ruling exists to state, and it was withdrawn on 2026-09-08 rather than reformulated.* What is **not** open, and needs no ruling, is that a year cannot be obtained by adding its months: §12's truncation settles that alone. Not reachable — neither yearly route exists and `assertReportingPeriodHasBegun` takes a month rather than a year. Settle it with the yearly slice, which is the first thing that must name a bucket. Raised by `architecture-guardian` on the fix batch for decisions 0221 and 0222.
- **Whether an archived or merged disciple is a coverage obligation, and at which instant lifecycle is read.** **A Stop Condition.** §9 names exactly one exclusion from a coverage denominator — the Network roots — and says nothing about a leader whose disciple has since been archived or merged. The argument for excluding them is real and is not a rule: the roster drops both and the submission refuses both, so a leader whose only disciples are archived can record nothing and would sit in the denominator with no act available to move them out, which is the one thing §15 says an attention list must never be. The argument for keeping them is §20's, that coverage attributes by the **obligation** rather than by the record. The first build of `DccCoverageService` took the exclusion and reached it through `forDecisionsWithin`, which reads the **current** lifecycle row against a dated edge set — so archiving one disciple today moved a past Sunday's figure from `1 of 1` to `0 of 0`, which §3 forbids in terms. The filter is now gone and `dcc-events.e2e.spec.ts` pins the figure against an archive, so the code follows §9 as written; what is open is whether §9 should say something narrower, and if it should, whether lifecycle is read as of the event date rather than as of now. §3 supplies the instant if the answer is yes — `person_lifecycle` is effective-dated exactly so that question is "a query" rather than "a hope" — and a merge may well differ from an archival, since §3 makes a merge "a statement about identity that was always true" while archival applies forward from its date. **§9 states both sentences and the code cannot honour both**, which is the shape of the question rather than a fault in either arm: it names one exclusion from a coverage denominator, and it promises of this very route that "each entry offers the action that resolves it". Whichever way it is settled, one of those sentences is amended. **§5 bears on it and may make most of the class unreachable**: it describes an archived Person as one "whose assignment has ended", so once archival is built the edge closes and a *current* event owes nothing — leaving only the past events the person's leader owed while they were still assigned, which §3 says must not move anyway. Not blocking; the behaviour is defensible and follows the one sentence §9 states about denominators. Raised by `architecture-guardian` on the index and attention routes, and sharpened by its review of the fix batch, which established that "follows §9 as written" was true of one half of §9 only.
- **What a Cell listing answers for a month that has not begun, and whether two rows of `cell_categories` or `cell_schedules` may be in force at one instant.** **A Stop Condition**, and two halves of one route's exposure rather than two questions that happened to arrive together. §15 states the row shape with no alternative — "the month's coverage line as **two figures**" — while §20's own heading says a period that has not begun is not reportable and refuses the request as `VALIDATION_FAILED` (decision 0216), adding that §17's open flag "cannot correct it". Neither sentence names a *list of Cells*, and `GET /api/v1/cells` is guarded by `cell.view_subtree` rather than by `reports.view_subtree`, so whether it is a report in §20's sense is undecided. **It ships refusing**, which is the conservative arm and which follows the nearest existing ruling rather than inventing a third answer — a fix batch briefly shipped `200` with a null coverage line and `open: true`, which is exactly the state §20 says its flag cannot rescue. The second half arrived with the fix for it: joining those two tables on the instant rather than on `ended_at IS NULL` moved the row-uniqueness this route depends on off a database constraint, since `cell_categories_one_open` and `cell_schedules_one_open` are partial over open rows and nothing refuses a historical overlap. The remedy is the `EXCLUDE USING gist (cell_id WITH =, tstzrange(started_at, ended_at) WITH &&)` the `pastoral_assignments` and `network_assignments` bullets above already propose, which would make these the third and fourth tables in that class. Not reachable: both change paths close before opening. **`cell_leaderships` is a fifth member of the class, and a narrower one than the others.** `cell_leaderships_one_open_per_cell` is `UNIQUE (cell_id) WHERE ended_at IS NULL`, so no index refuses a historical overlap, and a dated leadership join over one returns **two rows for one Cell**. **It cannot reach the Cells index, and the reason is worth keeping**: the overlap commits only on a **CLOSED** Cell, and that list is `ACTIVE`-only. On an `ACTIVE` Cell the contiguity check refuses it, because the successor must stay open and `assert_leadership_stays_in_network` is deferred — it re-reads the row at commit and only returns early when it finds it closed, which an `ACTIVE` Cell forbids. Reproduced against `dfc_ci` by a **committed** transaction, which is what it takes: the check that would refuse an overlap is a `DEFERRABLE INITIALLY DEFERRED` constraint trigger, so a rolled-back probe shows two rows whether or not the schema would accept them and proves nothing. *A first version of this bullet cited a rolled-back one, which is evidence that cannot reach its own conclusion.* **Nor is the partial index the whole reason.** Migration 0009 refuses the obvious route directly — `assert_leadership_stays_in_network` requires a successor to start exactly where its predecessor ended, and `cell_leadership_is_opened_open` refuses both an already-closed insert and a second write to `ended_at`. The overlap is reachable through one gap: a row inserted open and closed inside the same transaction is read back closed at commit and skips the contiguity check by its early return. So a reader reproducing by the obvious route is refused and concludes this bullet is wrong. The `EXCLUDE USING gist` remedy above closes it with the rest. **A third face of the same axis arrived with the pass after that**: the DCC events index answers `open: true` for a month that has not begun, which §20 says §17's flag cannot correct — so one of the two grounds given for refusing that month on the Cell listing is left standing on the sibling route with no argument. It binds every route carrying the flag rather than these two, which is why it is recorded here rather than settled in decision 0229. Raised by `architecture-guardian` on the second and third fix batches for the index and attention routes.
- **Where a scheduled meeting whose Cell had no leader on that date is placed.** **A Stop Condition**, and it arrived by a ground being withdrawn rather than by a behaviour changing. The Cell coverage denominator `LEFT JOIN`s the leader, so such a pair survives with a null leader and is counted at `CELL` and `WHOLE_CHURCH` scope and in no `LEADER` one — which puts it in the Whole Church total and in nobody's subtree, an additivity gap. §12 grounds **carrying** the row rather than dropping it, because "recording less must never look better", and says nothing about placing it. §20 states its additivity claim and its residuals about *a person being placed*, and says in terms that "coverage is not settled by that generalisation" and that it touches "neither instant, nor those fallbacks' application to either" — so the residual the code's docblocks used to cite was never lent, and withdrawing that citation is what left the placement ungrounded. Unreachable by construction: a Cell's schedule and its leadership are opened together at creation and closed together at closure, so a day with a schedule in force has a leadership in force. An inner join would be shorter and is the wrong fix — it would make a data defect shrink the denominator, which is the one direction §12 forbids. Settle it by having either §12 say where an unplaceable pair lands or §20 state a residual for the coverage key. Raised by `architecture-guardian` on the fix batch answering the coverage slice's mandatory review.
- **Which instant within a meeting date a Cell coverage subtree walk resolves at.** **A Stop Condition**, and the second question the coverage slice answered by itself — the first was escalated and settled as decision 0230. §20 fixes coverage's instant only as "each event or meeting date", decision 0221 declined to refine it, and §12 and §13 say nothing, so the two halves of one change took two answers. The DCC half walks at `recordingInstant`, **the earlier of now and the end of the event's Manila day**, which is the instant §9's own recording path uses; the Cell half walks at `endOfManilaDay(scheduledDate)` unconditionally, which is the instant decision 0208 adopted for reporting. **The leader half of the Cell denominator raises no such question**, because `leaderOnDateWithin` compares Manila *dates*; only the subtree walk needs an instant, which is why the divergence sits in one of the two and is invisible in the other. **No figure differs today, and the first version of this bullet claimed one did.** `subtreeAsOf` selects rows in force at the instant, so the two answers can only diverge on a row whose `started_at` or `ended_at` falls between now and the end of the day — that is, one dated in the future at query time. Nothing can write one: there are two inserters of `pastoral_assignments`, the reassignment path refuses a future effective date in terms ("An effective date is a correction to the past"), and the opening path stamps the encoding instant. *The withdrawn sentence said a report run this morning would place a Cell's leader "by a reassignment nobody has recorded yet", which is incoherent as well as false — an unrecorded write moves no query. It was refuted by `architecture-guardian` reading the two writers, in the sentence whose whole job was stating the cost.* Neither answer is obviously wrong: the day's end is what §13 already uses at the closure boundary and what decision 0208 adopted, and the clamped instant is what §9 uses everywhere a record is written. Not blocking, and unreachable rather than merely rare — which is the reason to settle it in the specification rather than to wait for a figure to go wrong. Settle it by having §20 name the instant for both domains rather than leaving each slice to choose — and note that whichever way it goes, one of the two shipped halves changes. Raised by `architecture-guardian` on the mandatory review of the coverage slice.
- **Which capability may read a Cell meeting's recorded marks.** **A Stop Condition**, and two live statements give two answers. **§7 itself** says a meeting's roster is guarded by "`cell.take_attendance` for a submission and **`cell.correct_subtree` for a correction**, resolved against the meeting", in its capability-boundaries list; the route declares the first alone; *and this bullet attributed that sentence to §22 for one commit, which softened the finding — the divergence is §7 against itself and against the route rather than §7 against a neighbour, and whoever settled it from the bullet would have grepped §22 and found nothing;* and §7's re-derivation of 2026-09-09 asserted that the roster and the submit route admit the same actors *and that all of them may overwrite the record*, which is false — correcting a record that already stands needs `cell.correct_subtree`, and `architecture-guardian` reproduced a `200` carrying `{"present":true}` for an account holding only `cell.take_attendance`, beside the `403` its own suite already pins for the same actor submitting. **The overstatement is one §7 had already made and withdrawn for DCC**, four paragraphs down, and the Cell paragraph reintroduced it. Until 2026-09-09 the divergence decided nothing, because the roster carried no attendance; the field decision 0223 added is what makes it decide who reads the marks — and that field exists to serve the correction case. The third option nobody has costed is to carry `record` only for an actor holding `cell.correct_subtree` over the meeting, which the service can already answer, `assertMayCorrect` being exactly that check. *A third answer stood in §7's re-derivation until it was withdrawn on 2026-09-09, and this bullet counted it for one commit.* Decision 0223 settles that the roster carries the marks and deliberately does not settle this. Not blocking: no reader reaches the route who could not reach it before, and the alternative — withholding the marks — is the silent data loss 0223 exists to close. Settle it with the correction screen, which is the first thing that will hold both capabilities in one hand. Raised by `architecture-guardian` on the roster implementation.
- **Whether an index row may link to a route that refuses it.** Decision 0231 dates the membership of `GET /api/v1/cells`, so once the route is changed a leader who handed a Cell over will see it listed for a month they oversaw, while **every** detail route under `/cells/{id}` refuses them — all eight resolve through the Cell’s current leader. **One of the eight refuses under the very capability the index is granted on**: `GET /api/v1/cells/{id}/members` carries `cell.view_subtree` exactly as the index does and is undated only because it names no period, so this is not two capability classes resolving differently by design and must not be read as though it were. `GET /api/v1/cells/{id}/meetings` is the one first noticed, and that route carries `cell.take_attendance`, a recording capability, which §7 resolves through the Cell’s **current** leader. **This is not the open closed-Cell-fallback question**, and it is recorded separately so it cannot be mistaken for it: that one is about a **closed** Cell and a fallback firing, and this is an **ACTIVE** Cell where no fallback arises and the current-leader resolution simply names somebody else. Three answers are defensible: date the meetings route, which §7 declines by capability class; accept the refusal and say so on the screen before the click; or list such a Cell without a link. Not reachable until the code change lands, and the change should not land without an answer, because the screen is already shipped. Raised by `architecture-guardian` on decision 0231.
- **Whether the actor’s own scope walk and its Network enumeration date with the collection.** **A Stop Condition.** Decision 0231 dates which Cells a period’s membership contains and says nothing about how the *actor’s* subtree is resolved. `AuthorizationService.scopeMembership` walks `HierarchyService.subtreeOf`, which selects `WHERE pa.ended_at IS NULL`, and resolves a `NETWORK` grant at `new Date()`. So a leader who left the actor’s subtree in September still yields the actor no August Cells, which is on the graph axis the defect decision 0207 removed on the date axis. It also bites on §7’s own sentence that a `NETWORK` grant "follows that same instant, and this binds all three dated capabilities", which `new Date()` does not honour — and that sentence still describes `cell.view_subtree` as having an "undated route", written when the capability had one. Escalated rather than decided: dating a subtree walk is an authorization change and decision 0231 deliberately did not make one. Raised by `architecture-guardian` on decision 0231.
- **Whether a collection’s `ACTIVE`-state filter dates with its membership.** **A Stop Condition**, and the two live statements disagree. §7 now says a collection "resolves its membership as of that period" with no carve-out, while decision 0231 carves one out in terms, saying the `ACTIVE`-only filter is untouched and that folding it in would have been a second ruling wearing the first one’s name. A Cell closed on 5 September *was* in the collection at the final instant of August, and §19 wants a closed Cell’s meetings reachable while their month is open. `cells-index.e2e.spec.ts` pins the current answer. SKILL.md is the source of truth, so an implementer following it ships what the ruling refuses — which is the shape to settle before the code moves rather than after. Raised by `architecture-guardian` on decision 0231.
- **Which instant a Cell’s category and schedule joins take, once its membership is dated.** **A Stop Condition**, and it is a membership question wearing a display question’s clothes. `cellsInScope` joins `cell_categories` and `cell_schedules` at the caller’s `now`, and both are **inner** joins — so their instant decides which rows exist at all, not merely which category is shown. A Cell created after the period has neither row in force at the period’s instant and would vanish from the list under a dated join, which is a second membership change decision 0231 does not name alongside the handover it does. Reproduced against the database: a Cell created 3 September lists for August today, carrying the `0 of 0` line decision 0225 requires to be shown, and returns no row under the dated join. Settle it with the code change, and note that whichever way it goes the answer must be stated for all three joins rather than for the leadership one alone. Raised by `architecture-guardian` on decision 0231.
- **Whether Cell leadership is contiguous, which decision 0220 says the schema enforces.** **A Stop Condition**, and it is a merged ruling’s supporting claim refuted by reproduction rather than by argument. Decision 0220 rests its enumeration of when the closed-Cell fallback fires on leadership being contiguous, "which the schema enforces (migration 0009)". It does not, quite, and the gap is exactly where 0220 is arguing. `assert_leadership_stays_in_network` requires a successor to start where its predecessor ended and refuses an overlap outright on an **ACTIVE** Cell — reproduced, refused. But it is `DEFERRABLE INITIALLY DEFERRED`: it re-reads its row at commit and returns early when that row is closed. So an overlapping successor that is closed before commit skips the check, and the transaction commits with two rows in force at one instant. That needs the Cell to end **CLOSED**, since an `ACTIVE` Cell must hold exactly one open leadership row. Reproduced against `dfc_ci` by a committed transaction, and the two intermediate refusals were reproduced too, which is what fixes the boundary. What that costs is **not** established here and is the reason this is escalated rather than filed as a defect: 0220’s enumeration concerns when the fallback fires, and the case its argument actually needs is a **gap** in leadership rather than an overlap, which was not tested. Whoever settles it should test the gap case before deciding whether 0220’s conclusion moves at all, and should note that the `EXCLUDE USING gist` remedy the overlap bullet above proposes would make the claim true rather than merely restating it. Raised by `architecture-guardian` on the second review of decision 0231.
